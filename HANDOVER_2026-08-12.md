# 引き継ぎ 2026-08-12（イノウエ側Claude / Sonnet）

前回の引き継ぎ書: `HANDOVER_2026-07-23.md`（2026-07-23、Mac側Claude）
このセッションで着手前に必ず読むこと。特に「4. 未解決・要フォロー」は次にやることの起点。

---

## 背景：河﨑さんが作った `app/`（RSS+GitHub-JSON版）を引き継いで作業

河﨑さんから「GASを使わず、公開RSSフィード＋GitHub直置きJSONに変える高速化版を`app/`に作った」という共有があった（本ファイル末尾に原文を残す）。今回はその`app/`の**データ移行の完全性検証**から着手し、見つかった欠落を埋める作業を行った。

## 1. データ移行完全性の検証結果と対応（Phase A）

`app/`のRSSベースのデータと、本番（GAS/スプレッドシート）のデータを実データで突き合わせたところ、以下の欠落が見つかった。

- **shownote本文がRSSとスプレッドシートで別物**：RSSの`<description>`は公開時点の生テキスト、スプレッドシートの`shownote`列は公開後に人力で加筆（タグ追加・`[img:key]`画像埋め込み）されたバージョン。全21話中14話でタグが不一致、画像埋め込みはRSS側に0/3
- **手動リンク（`app/links.json`）が未移行**：本番の承認済みリンク10件のうち、`app/links.json`にはテストデータ2件しか入っていなかった

### 対応
- `scripts/snapshot-shownotes.mjs`：GAS APIから全21話のキュレーション済みshownote本文と`images`（`[img:key]`用のkey→Drive fileIdマップ）を取得し、`app/shownotes.json` / `app/images.json`に書き出す（`npm run snapshot`で再実行可）
- `app/data.js`：`applyCurated()`で、RSS由来のエピソードに対しフィールド単位（`descHtml`/`tags`/`link`）でキュレーション本文を上書き。存在判定は常にRSS優先、本文はキュレーション優先＋無ければRSSにフォールバック
- `app/links.json`：本番の10件で置き換え
- `scripts/verify-parity.mjs`：検証スクリプト。`npm run verify`でエピソード数・タグ一致・画像・リンク数をチェック（全項目PASS確認済み）
- 副次的なデータ修正：ep9のスプレッドシート側urlが番組トップURLの入力ミスだったため、`shownotes.json`側で空にしてRSSリンクにフォールバックさせた

## 2. タグ抽出バグの修正（`#` と `##` の区別）

`app/data.js`のタグ抽出が`#`の連続数を区別しておらず、`##`（未タグ候補・スプレッドシート上の運用ルール）まで通常の`#`タグと同じにネットワークグラフへ出していた。実データで確認したところ、本来表示すべき`#`タグは7個だけなのに、`##`（24個）が混ざって31個のグレーノードになっていた。

`TAG_RE`を`/#+(...)/g`に変更し、`m[0].match(/^#+/)[0].length !== 1`のものを除外するよう修正（`concepts.js`/`episodes.js`/`untagged.js`が元々持っていたのと同じロジック）。

**注意**：この過程で「本番(GAS)側にも同じバグがあるのでは」と一時的に疑ったが、後述の`gas/Code.gs`同期で確認した結果、**本番の実デプロイ済みコードは元々正しく実装されていた**ことが判明。誤情報だったので訂正済み（`QUESTIONS.md`参照）。

## 3. about / contact / log ページを `app/` へ移植

- `app/about.html`, `app/contact.html`：rootの静的ページをそのまま移植（`page.css`は`../`参照で共有、複製していない）
- `app/log.html`, `app/log.js`：後述のLOG自動化とセットで移植
- `app/logo.js`：app/用に画像パスのみ調整したロゴコンポーネントを新設（rootの`logo.js`とは別ファイル。理由：JS内の相対パスはCSSと違い「ページのURL基準」で解決されるため、app/配下からは`../image/`にする必要があった）
- `app/nav.js`：ABOUT/LOG/CONTACTをapp内の新ページへリンク。TOP/EP LISTは、index.html上では従来通りページ内タブ切替、それ以外のapp/ページからは`index.html`（EP LISTは`?view=table`付き）へ通常遷移するよう修正
- `app/graph.js`：`?view=table`クエリでテーブルビューを直接開けるように対応（nav.jsの新しいEP LISTリンクの遷移先）
- CONCEPTS/INSPIREDはまだ未移植（root版のまま、`app/nav.js`から`../concepts.html`等にリンク）。理由は次項

## 4. `gas/Code.gs` 同期（重要な地雷を発見・除去）

リポジトリの`gas/Code.gs`が、**実際にデプロイされているコードより大幅に古い**ことが判明した。CLAUDE.mdの手順（`gas/Code.gs`を更新したらGASエディタにコピペ→デプロイ）をそのままやると、**実デプロイ済みのLOG機能・概念/作品ページ機能が丸ごと消える**危険な状態だった。

河﨑さん側にGASエディタの最新コードを直接貼ってもらい、それで`gas/Code.gs`を完全同期した。この同期で分かった、リポジトリに無かった機能：

- `buildLogsData()`：LOGページ用API（`?type=logs`）。既に動いていた
- `buildConceptsData()` / `syncConceptsSheet()`：CONCEPTSページ用。ショーノートの`#`/`##`から概念を自動計算し、`concepts`シート（手動列：description/reading/proposer/related_concepts等）とマージ。関連・対比概念の双方向リンクあり
- `buildWorksData()` / `syncWorksSheet()`：INSPIREDページ用。ショーノートの絵文字マーカー（📚🎬📺🎵📻）から作品を自動抽出し、Google Books API / iTunes Search APIで著者・表紙画像を半自動取得
- `CacheService`によるレスポンスキャッシュ（type毎に10分）

**→ CONCEPTS/INSPIREDの`app/`移植は、単純な静的化では済まない**（サーバー側の計算ロジックが本体）。着手前に設計から検討が必要。次チャットでの優先課題候補。

**教訓（運用ルールに追加すべき）**：`gas/Code.gs`は手動デプロイの性質上、リポジトリと実体がズレやすい。GAS側を触る・参照する作業の前には毎回最新コードを貼ってもらって同期確認すること。

## 5. LOGのGitHub Actions自動スナップショット化

ユーザー（イノウエ）から「LOGの保存方式もエピソードと同じにできないか」という提案があり検討した結果：

- 単純に同じ方式（手動`npm run snapshot`）にすると、フォームで書いてもPC作業が必須になり、LOGの「思い立ったら書く」という運用に合わない
- 応答速度を実測：GAS直叩き約1.9〜2.3秒 vs 静的JSON約0.3〜0.6秒。差は無視できない
- **採用案**：GitHub Actionsで30分おきに自動スナップショット。ユーザーの操作は今まで通りフォームに書くだけ、PC操作は一切不要

### 実装
- `scripts/snapshot-logs.mjs`：GASの`?type=logs`から`app/logs.json`を生成。0件取得時は上書きしない安全装置あり
- `.github/workflows/sync-logs.yml`：30分おきの`cron` + `workflow_dispatch`（手動即時実行も可）。差分が無ければコミットしない
- `app/log.js`：`app/logs.json`を読む版に変更（**root版の`log.js`はGAS直叩きのまま。変更していない**）
- `LOG_ARCHITECTURE.md`：初心者向けの仕組み説明。ユーザーから「時々聞くのでmdにしておいて」と要望されたため作成

**⚠️ 河﨑さんの意図との差分（要すり合わせ）**：河﨑さんのメッセージでは「LOGも同じ仕組み（GitHub直置きJSON＋admin.htmlと同じパスワード付きフォームで直接投稿）に将来統合したい」とのこと。今回実装したのは「スプレッドシート＋GASを裏で残しつつ自動同期する」方式で、**最終形が違う**。河﨑さんの案は「GAS完全廃止」まで行けるが、フォームを新規開発する必要がある。今回の案は「GASはまだ裏に残るが、閲覧時の依存は消える」段階的なもの。どちらで進めるか、または今回の方式を中間ステップとして残すか、相談が必要。

**→ 同日中に決着（6節参照）。ユーザーが河﨑さん案を希望し、その場で置き換えた。** 上記5節の方式は当日中に廃止されているので、次にこのファイルを読む人は6節を正として扱うこと。

## 6.【同日追記】LOGをGitHub直書き方式に統合（河﨑さん案で最終決着）

5節の「GAS+GitHub Actions自動同期」案について、ユーザーに「河﨑さんの狙い通りGitHub直置きJSONにしつつ、投稿の手軽さを残せないか」と聞かれた。`app/admin.js`（links.json編集用）を読み直したところ、**OAuthではなく「合言葉→暗号化トークン復号→GitHub Contents APIで直接コミット」という、もっと単純で既に動いている仕組み**だと判明。合言葉はブラウザに保存され次回以降は自動ログインするため、Googleフォームとほぼ同等の手軽さで実現できると判断し、その場で実装・置き換えた。

### 実装
- `app/logpost.html` / `app/logpost.js` / `app/logpost.css`：新規投稿・編集・削除ページ。`admin.js`と**同一のTOKEN_BLOB・同一の合言葉（`mokumoku`）**を流用（新しい秘密は発行していない）。GitHub Contents APIで`app/logs.json`を直接読み書き
- 新規投稿時：既存最大id+1を自動採番、保存前に日付降順へ自動ソート、投稿者は「イノウエ」「カワサキ」の選択式
- 遊び心：投稿成功時にランダムな一言メッセージ＋絵文字が浮かび上がる演出（`popPuffs()`）。ユーザーから「書きやすく、修正しやすく、ちょっと遊び心があると嬉しい」との要望あり
- `app/log.html`に「＋ もくもく投稿する」リンクを追加（`logpost.html`への導線）
- **廃止**：`scripts/snapshot-logs.mjs`、`.github/workflows/sync-logs.yml`（5節で作ったGAS自動同期の仕組み。直接投稿に一本化したため、放置すると30分おきに新しい投稿を古いスプレッドシート内容で上書きしてしまう危険があったため削除した）
- `LOG_ARCHITECTURE.md`：最終形（GitHub直書き）に合わせて全面更新
- 検証：GitHub APIをモックしたjsdomテストで、合言葉ログイン（本物の復号ロジック）・新規投稿・id採番・ソート順・削除の一連の流れを確認済み（実際の書き込みはテストでは行っていない）

### root側LOGへの影響
`log.html`（root、本番公開中）は今まで通りGAS直叩きのまま変更していない。ただし今後は誰もスプレッドシートに追記しなくなるため、root側LOGは内容が更新されなくなる（過去ログとしてはそのまま残る）。`app/`側が今後の本番になる想定なので想定内。

## 現在の運用ルール（`HANDOVER_2026-07-23.md`から継続、今回追加分は◆）

- 本番公開ブランチは`feature/logo-deco`（mainではない）
- ルート直下の`index.html`/`graph.js`/`style.css`など（`app/`フォルダ外）は変更しない。`app/`配下のみ追加・変更する
- `app/links.json`と`app/table-layout.json`はユーザーが`admin.html`/`layout.html`から直接PUTで更新できる。作業前に必ず最新版をcurlで確認し、ユーザーの保存値を上書きしない
- ◆ `gas/Code.gs`はリポジトリと実デプロイがズレやすい。触る前に最新コードを貼ってもらって同期確認する
- ◆ commit/pushしたら同じタイミングでHANDOVERファイルも更新する。相手はpullするまで見えない前提で運用する

## 次にやること（優先順位は未整理）

1. ~~河﨑さんとLOGの最終形をすり合わせる~~ → 6節で解決済み（GitHub直書き方式に統合）
2. CONCEPTS/INSPIREDページの`app/`移植（設計検討から。3節参照）
3. `app/`のtable-layout.json周りの微調整（河﨑さん側で継続中の可能性あり、着手前に最新版を確認）
4. `QUESTIONS.md`の残項目（GAS_URLの最新デプロイ一致確認／「参考番組」シートの用途）
5. `logpost.html`の実際の初回投稿を、ユーザー自身の画面で動作確認してもらう（今回はモックfetchでのロジック検証のみ。実際のGitHubへの書き込みは未確認）

---

## 参考：河﨑さんからの共有メッセージ原文（2026-08-12にユーザー経由で共有）

> WEBを高速化してみました！公開されてるものと別で、非公開で作ってみてます。
> https://luckymokumokuchance.github.io/podcast-graph/app/index.html
> GASを使わないようにして、公開されてるRSSフィードから情報を取ってくるように変更しました。
> エピソード間のリンクなどRSSにいれてないものは、githubに直置きのjsonファイルで保存するようにしました。これはWEBの右上ボタンから登録可能で、この登録フォームのアクセスにはパスワード"mokumoku"が必要です。
> あとネットワークビューとテーブルビューの切り替え出来るようにしたく、今簡単なテーブルビューを追加してます。これはこれからブラッシュアップして、all episodeはこれに統合したいです。
> 井上さんに作ってもらったLOGデータなんかも同様のシステムで保存できるはずなのでおいおい統合したいです！
> DBがスプシから、RSSとgithub内のjsonファイルに変わるという形です！
