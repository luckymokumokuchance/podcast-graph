# 設計図：RSSをDBにする

作成日: 2026-09-17
決定者: イノウエ（方向性）／ Opus（技術設計）

---

## 目的

| | 今 | これから |
|---|---|---|
| 新エピソードを出す | Spotifyに上げる **＋** スプレッドシートに全文を貼る | **Spotifyに上げるだけ** |
| 後から概念を足す | スプレッドシートの全文を編集 | ●ボタンから1行足す |
| DB | スプレッドシート（GAS経由） | RSS ＋ GitHub上のJSON |

---

## 調査で分かったこと（2026-09-17）

### ✅ 理想はすでに半分動いていた

第026・027回は**スプレッドシートに行が無いのに、サイトで正しく動いています**。

```
第026回「デジタルガーデン」
   タグ: 味噌づくり / ゆる継承 / デジタル民藝 / 自由研究 / 非平衡
第027回「トークテーマ消費回」
   タグ: 本物っぽさ
```

RSS説明文28話中17話に、すでに `#タグ` が書かれていたためです。
**「Spotifyに上げたら反映される」は、気づかないうちに成立していました。**

### ❗ スプレッドシートは「重複」ではなく「後から足した層」だった

RSSだけにすると **19の概念が消えます**。

> 読書メモ / フレームワーク / 檸檬 / 宝物 / 進化心理学的なギャップ / アンビエントビジュアル /
> ものの探し方の流派 / 本の読み方の流派 / ジスベリ / ボムりたい / 電話体ラジオ / タグ管理問題 /
> ジョグ型編集 / スプリント型編集 / その人性 / 無機物への命名欲求 / 自己矛盾の肯定 /
> コンセプト文 / 誠実さと射程の長さ

これらは**収録直後には存在しなかった概念**です。後から振り返って `##` を足したもの。
作品の参照リンクも同じで、RSSは `📚電気羊はアンドロイドの夢を見るか?／フィリップ・K・ディック` 止まり、
URLはスプレッドシートにしかありません。

### → 負担の正体

**「後から少し足すために、全文を貼り直していること」**
足したいのはタグ1個なのに、1700字の本文を丸ごとコピーしている。これが重い。

---

## 設計

```
┌─ 公開時に書く ───────────────────────────┐
│  Spotifyの説明文                                    │
│    概要 / [トピック] / #タグ / 📚作品                 │
│          ↓ RSS（自動）                              │
│  ✅ ここは既に動いている。仕組みを足さない             │
└────────────────────────────────────────┘
                     ↓ 重ねる（追記だけ）
┌─ 後から足す（●ボタンの向こう） ────────────┐
│  app/episodes-extra.json … ##概念・作品リンク        │
│  app/concepts-meta.json  … 概念の説明・提唱者・関連   │
│  app/works-meta.json     … 表紙画像                 │
│  app/images.json         … 本文中の画像             │
└────────────────────────────────────────┘
                     ↓
                 サイトに表示
```

### 核心：後から足す層は「追記だけ」にする

`app/shownotes.json`（全文コピー）を捨て、`app/episodes-extra.json`（差分だけ）に置き換えます。

```json
{
  "22": {
    "addTags": ["読書メモ", "##ジスベリ"],
    "workLinks": {
      "book|電気羊はアンドロイドの夢を見るか?": "https://ja.wikipedia.org/wiki/..."
    }
  }
}
```

本文は持ちません。**新エピソードはこのファイルに登場すらしません。**

---

## 決定事項

### ① 定型フッタは自動で除去する

RSS説明文の20/28話に、以下の定型文が入っています。サイトに出す意味がないので機械的に取り除きます。

```
●毎週火曜朝8時の基本週1回配信
●気になるタイトルからチェックすべし／どこから聴いてもOKです
●ご感想は #ラキもくチャン #ラッキーもくもくチャンス でお願いします
●お頼り・ご依頼などはHPもしくはメールでお願いします
✉️lucky.mokumoku.chance@gmail.com
番組HP：...
```

Spotify側には残したままでOK（リスナーには必要な情報なので）。**サイト表示時だけ落とします。**

### ② 作品記法は `／` で著者を切り出す

RSSでは `📚題名／著者` の形で書かれています。今のコードはこれを丸ごとタイトルとして拾ってしまい、
INSPIREDページに `Tシャツが乾くまで／生方美久` という作品名が出ています（026/027で発生中）。

`／` `/` `・` で区切って、タイトルと著者に分けます。

| 書き方 | 結果 |
|---|---|
| `📚題名／著者` | タイトル=題名, 著者=著者 ← **新規対応** |
| `📚[題名](URL)／著者` | 従来どおり（URLも取れる） |
| `📚題名` | タイトルのみ |

### ③ 本文の上書きは「非常口」として残すが、原則使わない

**本文を直したくなったら、Spotify側の説明文を編集してください。** RSSが自動で追従します。

`bodyOverride` という欄は用意しますが、通常は空のままです。
Spotifyでは表現できない事情が出たときだけ使う逃げ道として置いておきます。

### ④ リンクはSpotifyにHTMLで書く（●での登録は不要）

**SpotifyはHTMLが書けます。** そしてRSSはそれをCDATAでそのまま運び、サイト側の `marked` は
HTMLを素通しするので、**HTMLが文字として見えることはありません**（検証済み）。

```
Spotifyに書く:
  📚<a href="https://ja.wikipedia.org/wiki/...">電気羊はアンドロイドの夢を見るか?</a>／フィリップ・K・ディック

Spotifyでの見え方:   📚電気羊はアンドロイドの夢を見るか?（リンク）／フィリップ・K・ディック
サイトでの見え方:     同上（target="_blank" も自動付与される）
```

実際、**28話中16話のRSS説明文にはすでに `<a href>` が入っています**（フッタの番組HPリンク）。
HTMLが漏れている箇所はゼロでした。

#### ただし1つ落とし穴がある（対応必須）

作品の抽出は「HTMLタグを外したテキスト」を見ているため、**このままだとリンクのURLが捨てられます**。

| 書き方 | 現状の抽出結果 | 修正後 |
|---|---|---|
| `📚<a href="U">題</a>／著者` | 題=「題／著者」 URL=なし ❌ | 題=「題」 URL=U 著者=著者 ✅ |
| `📚題／著者` | 題=「題／著者」 ❌ | 題=「題」 著者=著者 ✅ |
| `📚[題](U)／著者` | 正常 | 正常（維持） |

**対応**：HTMLの `<a>` を先に拾ってURL・題名・著者を取り、残りをプレーン処理する二段構えにします。
これで3つの書き方すべてが正しく扱えます（プロトタイプで検証済み）。

→ **結果として、●でURLを登録する作業は不要になります。** Spotifyに書けば全部つながります。

---

## 進め方

### Phase 1｜表示・抽出の改善（移行なし・低リスク）
- 定型フッタの除去
- 作品抽出をHTML対応の二段構えにする（`<a>`からURL・題名・著者を取る）
- `／` による著者の切り出し

→ この時点で、026/027のような**スプレッドシート未登録の回が正しく見えるようになります**。
→ 以後、**Spotifyの説明文にHTMLでリンクを書けば、それだけでINSPIREDにもURLが載ります**。

### Phase 2｜追記層への移行（ここで目的達成）
- スプレッドシートとRSSの差分を機械的に抽出し、`episodes-extra.json` を生成
  （19の概念・作品リンクは自動で移ります。手作業なし）
- `app/shownotes.json` を廃止
- `.github/workflows/sync-shownotes.yml` と `scripts/snapshot-shownotes.mjs` を削除

→ **新エピソードはSpotifyに上げるだけになります。**

### Phase 3｜編集画面（GASの引退）
- `app/curation.html` … 概念・作品・追記層を●から編集
- `concepts-meta.json` / `works-meta.json` がスプレッドシートから独立
- GASとスプレッドシートが完全に不要になる

---

## 移行しても失われないもの（確認済み）

| | 保持方法 |
|---|---|
| 19の`##`概念 | `episodes-extra.json` の `addTags` へ機械的に移行 |
| 作品の参照リンク | `episodes-extra.json` の `workLinks` へ移行 |
| 概念の説明・提唱者・関連・文脈 | `concepts-meta.json`（既に独立済み） |
| 作品の表紙画像 | `works-meta.json`（既に独立済み） |
| 本文中の画像 | `images.json` + `[img:key]`（サイト側のみ） |

## 注意点

- **`episodes.html`（Studio埋め込み）はGAS依存のまま**。Phase 3でGASを止める前に、この扱いを決める必要があります
- スプレッドシートは削除せず、記録として残します（全ての原文があるため）
- 過去回の本文をRSS側に寄せたい場合は、Spotifyの説明文を編集すれば自動で反映されます（必須ではありません）
