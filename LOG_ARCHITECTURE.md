# LOGページの仕組み（初心者向け整理）

作成日: 2026-08-06（初版） / 更新: 2026-08-06（GAS自動同期方式）→ 2026-08-12（GitHub直書き方式に統合）

---

## 結論（先に一言で）

**LOGの投稿場所が変わりました。** 今までのGoogleフォーム＋スプレッドシートをやめて、`app/logpost.html`という新しい投稿ページから直接投稿する形に統合しました。GAS・スプレッドシートを一切使わなくなり、河﨑さんが`app/links.json`向けに作った「合言葉で入って、GitHubに直接保存する」仕組みをLOGにもそのまま使っています。

---

## 1. 新しい投稿方法

1. `app/log.html`（LOGページ）の「＋ もくもく投稿する」リンク、または直接 `app/logpost.html` を開く
2. 初回だけ合言葉（`mokumoku`）を入力 → 次回以降は自動でログインした状態になる
3. タイトル・本文・投稿者（イノウエ／カワサキ）・日付を入力して「もくもくっと投稿する」
4. その場でGitHubに直接保存され、約1分でサイトに反映される

修正・削除も同じページの「投稿済みLOG」一覧からその場でできます（スプレッドシートを開く必要がなくなりました）。

## 2. 仕組み（今までとの違い）

```
[これまで]
  Googleフォームに書く → スプレッドシート「logs」シートに追記
        → 見る人がアクセスするたびGAS経由で取得（rootの log.html）
        ※一時期、GitHub Actionsで30分おきにGASからapp/logs.jsonへ
          自動コピーする方式を試したが、直接投稿方式に統合したため廃止済み

[今] ─ 2026-08-12〜
  app/logpost.html に直接書く
        ↓（ブラウザから直接）
  GitHub API 経由で app/logs.json（このリポジトリの中のファイル）に直接保存
        ↓
  app/log.html が app/logs.json を読んで表示（GAS一切不要）
```

`app/links.json`（エピソード間の手動リンク）を編集する`app/admin.html`と、まったく同じ「合言葉でログイン→GitHubに直接コミット」の仕組みを使っています。新しい秘密の合言葉を新たに用意したわけではなく、既存の合言葉をそのまま使い回しています。

## 3. root（本番公開中のサイト）側のLOGはどうなる？

`https://luckymokumokuchance.github.io/podcast-graph/log.html`（rootの方）は今まで通りGAS・スプレッドシート経由のままで、**変更していません**。ただし今後は誰もスプレッドシートに新しいLOGを追記しなくなるため、root側のLOGは今の内容で更新が止まります（過去ログの記録としてはそのまま残ります）。`app/`側が今後の本番になる想定なので、これは想定内の変化です。

## 4. 今後の運用

| 項目 | 内容 |
|---|---|
| LOGを書く場所 | `app/logpost.html`（旧：Googleフォーム） |
| 合言葉 | `mokumoku`（初回のみ入力、以後は自動） |
| 投稿者 | 「イノウエ」「カワサキ」から選択 |
| 反映タイミング | 保存直後、約1分でサイトに反映 |
| PCでのコマンド操作 | 不要（ブラウザだけで完結） |
| スプレッドシートへの追記 | 不要になった（過去分はそのまま記録として残る） |

## 5. 裏側の仕組み（技術メモ・読み飛ばしOK）

- `app/logpost.html` / `app/logpost.js` / `app/logpost.css`：投稿・編集用ページ。`app/admin.js`と同じ合言葉復号ロジック（AES-GCM + PBKDF2、同一の暗号化トークン）を流用し、GitHub Contents APIで`app/logs.json`を直接読み書きする
- 新規投稿時のid：既存の最大id + 1を自動採番。保存前に日付の新しい順へ自動ソート
- 廃止したもの：`scripts/snapshot-logs.mjs`、`.github/workflows/sync-logs.yml`（GASから30分おきに同期する仕組み。直接投稿に一本化したため不要になった）
