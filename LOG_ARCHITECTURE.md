# LOGページの仕組み（初心者向け整理）

作成日: 2026-08-06（初版） / 更新日: 2026-08-06（自動同期方式に変更）

---

## 結論（先に一言で）

**あなたがやること（フォームに書く）は今までと一切変わりません。** ただし裏側の仕組みを変えました。今までは見る人がアクセスするたびにスプレッドシートへ都度問い合わせていましたが、今は「GitHubが自動で30分おきにスプレッドシートを確認し、変化があれば静的ファイルとして保存する」方式に変えました。これによりLOGページの表示が速くなります（実測で約5倍）。

---

## 1. 今までの仕組み

```
[あなたの作業]
  フォームにLOGを書く
        ↓
[裏側]
  スプレッドシートの「logs」シートに1行追加される
        ↓
[閲覧者がLOGページを開くたび]
  GAS（Google Apps Script）に毎回問い合わせて
  最新のJSONを取得 → 画面に表示
```

この方式は「毎回スプレッドシートに聞きに行く」ため、閲覧のたびに約2秒待たされていました（実測）。

## 2. 今回変えた仕組み（2026-08-06〜）

```
[あなたの作業]  ← 変化なし
  フォームにLOGを書く
        ↓
[裏側・変化なし]
  スプレッドシートの「logs」シートに1行追加される
        ↓
[新設：GitHubが自動で30分おきに巡回]
  GitHub Actions が定期的にGASへ問い合わせ、
  変化があれば app/logs.json（静的ファイル）を自動更新・自動保存
        ↓
[閲覧者がapp/のLOGページを開くたび]
  GASではなく app/logs.json を直接読む（速い・実測0.3〜0.6秒）
```

**あなたが変えるべき行動は何もありません。** 今まで通りフォームに書けば、最大30分程度のタイムラグで自動的にサイトに反映されます。

## 3. 何が変わって、何が変わらないか

| 項目 | 変わるか |
|---|---|
| LOGを書く方法（フォーム） | **変わらない** |
| スプレッドシートへの追記 | **変わらない**（今まで通り自動で1行増える） |
| あなたがPCで何か操作する必要 | **無い**（今まで通り不要） |
| 反映されるタイミング | 即時 → **最大30分程度のタイムラグ**に変化（GitHub Actionsの巡回間隔のため） |
| ページの表示速度 | 約2秒 → **約0.3〜0.6秒**に高速化 |
| 表示される場所 | `app/log.html`（新しい高速ページ）。root側の`log.html`は今まで通りGASに直接問い合わせる方式のまま |

## 4. 今すぐ反映させたい時

「今すぐこのLOGを載せたい」という時は、30分待たずにGitHubの管理画面（Actionsタブ）から手動で同期を実行できます。手順が必要な時は聞いてください。

## 5. 裏側の仕組み（技術メモ・読み飛ばしOK）

- `scripts/snapshot-logs.mjs`：GASの`?type=logs`からデータを取得し`app/logs.json`に書き出すスクリプト
- `.github/workflows/sync-logs.yml`：上記スクリプトを30分おきに自動実行するGitHub Actionsの設定
- `app/log.js`：`app/logs.json`を読んで表示する（root版の`log.js`はGASを直接読む版のままで、こちらは変更していない）
- 取得0件（GAS側の一時的な不調など）の時は、既存の`logs.json`を上書きしない安全装置あり
