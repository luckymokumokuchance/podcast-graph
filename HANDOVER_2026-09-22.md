# 引き継ぎ 2026-09-22（イノウエ側Claude / Sonnet）— Studio関係の勘違い訂正

前回の引き継ぎ書: `HANDOVER_2026-09-19.md`

このセッションで「Studioとの関係」について何度か認識違いがあったため、
**正しい現状を1箇所にまとめておく。** 以後この件で同じ調査をやり直さないこと。

---

## 結論（3行）

1. **今のサイト（`app/`）は、Studioに一切依存していない。** コード上の参照はゼロ
2. **お問い合わせフォームはすでに`app/contact.html`に移管済み**（2026-08-06作成、Googleフォームへの裏送信で完結）
3. 残っている唯一の接続は逆方向：**Studio側の古いEP LISTブロックが、うちの`episodes.html`をiframeで覗いている**だけ。これはこちら側の機能に何の影響も与えていない

---

## 何を勘違いしていたか

このセッション中、以下の順で誤った説明をしてしまった。

1. 「`episodes.html`はStudio埋め込みのままGAS依存」という`CLAUDE.md`の記述を鵜呑みにし、
   「星図トップページがStudioと繋がっている」という趣旨の説明をした → **誤り**。
   星図（`app/graph.js`）に`studio.site`への参照は無い。参照が残っていたのは
   ルート直下の**使われていない旧`graph.js`**（`index.html`は`app/index.html`へ即転送するだけの殻）だけだった
2. 「お問い合わせフォームがStudio側にしか無い」と説明した → **誤り**。
   `app/contact.html`に、Googleフォームへ直接POSTする実装が既に存在していた（隠しiframeで裏送信）。
   古い引き継ぎメモ（`HANDOVER PAGES.md`、2026年8月分）に「これから作る」と書かれていたのを見て、
   その後の別セッションで実際に作られていたことを確認せずに古い情報のまま話してしまった

## 正しい現状（再確認済み）

| 項目 | 状態 |
|---|---|
| 星図・グラフ表示 | `app/graph.js`。Studio参照なし |
| ネットワーク⇔テーブル切り替え | `app/view.js` + `app/graph.js`（`enterTable`/`enterNetwork`）。同一ページ内アニメーション。すでに実装済み |
| ナビゲーション | `app/nav.js`。TOP/ABOUT/EP LIST(→`index.html?view=table`)/CONCEPTS/INSPIRED/CONTACTの6項目。**Studioも`episodes.html`も参照していない** |
| お問い合わせ | `app/contact.html`。Googleフォームへの裏送信で完結。Studio不要 |
| `episodes.html`（ルート） | 自分のサイトからは誰からも参照されていない孤立ページ。**Studio側の古いiframeブロックだけがこれを覗いている**。GAS依存（`episodes.js`）が残っているのはこのページだけ |

## 残っているタスク（優先度低）

- `episodes.html`を他のルートページ同様`app/index.html?view=table`への転送シェルに変える
  - これをやるとStudio側の古いEP LISTブロックの表示は壊れる。リスナー案内は既にGitHub Pagesなので実害は小さいが、
    実行前に「Studioのそのページが今も何かから踏まれていないか」を一度確認してからでよい
  - やれば`episodes.js`とそのGAS依存を完全に削除できる
- （任意）`episodes.html`にあった検索機能をテーブルビューに移植するかどうかは、上記が孤立ページである以上、緊急性は無い

## 教訓

- **古い引き継ぎメモ・design docの記述を、確認せずに「今の状態」として話さない。** 特に「〜する予定」と書かれている項目は、別セッションで実施済みになっている可能性がある。コード（`grep`等）で現物を確認してから説明すること
- ルート直下に残っている古いファイル（今回は旧`graph.js`）は、実際にどのHTMLからロードされているか確認するまで「今も有効」と決めつけない

---

## 新機能：画像登録（GAS/Googleドライブ不要）

### 背景

GASを卒業した結果、本文中の画像（`[img:key]`）を新しく登録する手段が無くなっていた。
今までは「Googleドライブに上げる → スプレッドシートの`images`シートにkey/fileIdを書く →
GAS経由で`images.json`に同期される」という、GAS依存の唯一の入力経路だった。

### やったこと

- **`app/imagepost.html` + `app/imagepost.js`** を新設。「●」→リンク編集／LOG投稿の並びに
  「画像登録」として追加した（合言葉ログインはadmin.js/logpost.jsと全く同じ方式）
- 使い方：話数を指定して画像ファイルを選ぶ → **画像そのものをリポジトリの`image/`直下に直接コミット**
  し、`app/images.json`にキー（例：`ep031_1`）を自動登録する。Googleドライブ・スプレッドシートは経由しない
- 発行されたキーを**コピーしてSpotifyの説明文の好きな場所に`[img:ep031_1]`と貼る**と、その位置に
  画像が表示される。`marked`でのレンダリング前に本文全体へ正規表現で置換しているため、
  貼った場所がそのまま表示位置になる（＝位置指定が効く）
- 古い回（もうSpotify側を編集しない回）向けに、`episodes-extra.json`の`addImages`（末尾に自動追記
  されるだけで位置は選べない）も互換のまま残した。新規の回は基本的に画像登録ツール経由が推奨

### コード変更

- `app/graph.js`のshownote画像展開（`renderShownote`内）：`images.json`の値が`/`を含む場合は
  リポジトリ内パス（新方式、`ASSET_BASE`を付けて解決）、含まない場合は従来のGoogle Drive fileId
  （旧回のぶんはそのまま動く）として分岐するように変更
- `app/data.js`の`load()`：RSSと`images.json`の取得に`?t=timestamp`＋`cache:'no-store'`を追加。
  今までは`images.json`にキャッシュ避けが無く、登録直後にブラウザキャッシュで反映が遅れる可能性が
  あったため（他の`links.json`/`episodes-extra.json`は既に対応済みだった）

### 確認したこと

- 画像src組み立てロジック（Drive fileId分岐／新パス分岐／未登録キーはそのまま残す）をnode上で
  単体テストし、意図通り動作することを確認済み
- `node --check`で変更した3ファイルの構文エラー無しを確認済み
- ブラウザでの実際の画像アップロード操作（GitHub APIへの実書き込み）は未検証。最初に使うときに
  一度動作確認してほしい

### 追記（2026-09-23）：pushした直後に出た2つの問題と対応

1. **「画像登録」リンクが見えない**：`.abar`/`.aright`（`app/admin.css`）に`flex-wrap`が
   無く、リンクを3本に増やしたことで狭い画面幅だと折り返さずに見切れる可能性があったため、
   `flex-wrap: wrap`を追加。あわせて`admin.html`はCache-Control: max-age=600なので、
   push直後だと古いキャッシュが10分ほど残ることもある（ブラウザの再読み込みで解消）
2. **ep029に画像を登録したのに表示されない**：原因は今回の新機能とは無関係で、**登録（`images.json`
   にキーを追加）と配置（`[img:key]`をどこかに書く）が別工程だった**ため。ep029は
   `images.json`に`ep029_1〜3`（旧GAS経由のDrive fileId）が入っていたが、配置先の指定が
   どこにも無かった（Spotify本文にも`episodes-extra.json`にも無し）。`episodes-extra.json`
   に`"29": {"addImages": ["ep029_1","ep029_2","ep029_3"]}`を追記して解決（本文末尾に表示）。
   jsdomで`app/data.js`を実際に動かしてdescTextに`[img:ep029_1]`等が入り、画像として解決される
   ことを確認済み
   - **教訓**：`images.json`にキーを足すだけでは画像は出ない。「①`images.json`にキーを登録」
     「②どこかに`[img:key]`と書く（Spotify本文 or `episodes-extra.json`のaddImages）」の
     2工程が必要、ということを次のHANDOVERやCLAUDE.mdでも強調しておくとよい

---

## 試して撤回：星図に作品ノードを追加（DG連携の下地・reverted）

DG連携（[[dg-integration-vision]]）の下地として、INSPIREDの作品(📚🎬📺🎵📻)を
星図にも「作品ノード」として追加し、引力弱め・リンク長めの物理設定＋EPタップで
関連作品が光る演出を実装してpushした（コミット`517ec5b`）。

**本番で確認したところNG**：「重い」「光らない」「文字（絵文字）も表示されない」と
報告があり、**`git revert`で丸ごと取り消した**（コミット`b01ff54`で復元）。現在は
このセッションで作品ノードを追加する前の状態に戻っている。

**原因は未診断。** このセッションではClaude in Chromeが無効化されており、実際の
ブラウザ描画・コンソールエラーを見られないまま実装・pushしてしまったのが根本的な
問題。jsdomでのデータ生成ロジック（`buildGraph()`が正しい形のノード/リンクを作れるか）
は単体テストで確認していたが、それはD3の物理シミュレーションやCSSアニメーションの
実際の見た目・パフォーマンスとは無関係で、そこが軒並み未検証だった。

**教訓**：D3の物理パラメータ調整やCSSアニメーションが絡む見た目の変更は、jsdomでの
ロジック検証だけでは「完了」と言ってはいけない。ブラウザ操作ツールが使えない状況では、
実際に目視確認できていないことを明示し、確認が取れるまで本番pushを待つ判断も必要
だった。再挑戦する場合は、まずローカルか検証用ブランチで実際の画面を見られる状態を
整えてから進めること。
