# 引き継ぎ書：ラッキーもくもくチャンス — 作品画像取得の修正＆概念の関係性拡張フェーズ

作成日: 2026-07-15
前回の引き継ぎ書: `HANDOVER_INSPIRED.md`直後の「後片付け＆概念ページ導線完成フェーズ」（同日・別チャット）

---

## 0. このチャットで何をしたか（サマリー）

大きく2つの作業を行った回。
1. **INSPIRED（作品一覧）の表紙画像がほぼ取得できていなかった問題の原因調査・修正**
2. **概念詳細ページ（`concept.html`）に「提唱者」「近接概念（自分たちで話した分／文献由来）」「対比概念」を追加**

あわせて、今後カワサキさんとルールを共有する方法についても方針を決めた。

### 到達点
- ✅ 表紙画像取得の3つの根本原因を特定・修正（Mixed Content／API呼び出し条件の設計ミス／`+`記号のURLエンコード崩れ）
- ✅ Google Books APIの1日クォータ切れ（429）に遭遇、原因特定まで完了。翌日以降に再実行予定
- ✅ `concepts`シートに`proposer`・`related_concepts`・`related_external`・`contrast_concepts`の4列を追加し、GAS・フロントエンド（`concept.html`/`concept.css`/`concept.js`）に反映済み
- ✅ 近接概念・対比概念の「双方向自動表示」ロジックを実装（片方の概念にだけ書けば、書かれた相手側にも自動で逆リンクが出る）
- ✅ 文献由来の近接概念（`related_external`）は引用文をそのまま書けるよう、区切り文字を「セル内改行」方式に変更（句読点・スラッシュが引用文中にあっても壊れない）
- ✅ ショーノート・概念関連の書き方ルールをまとめ、管理スプレッドシートに「書き方ルール」タブとして新設する内容を作成・提供済み（4章参照）

---

## 1. INSPIRED表紙画像 取得できない問題の調査・修正（重要・技術メモとして厚め）

### 症状
`inspired`シートの`creator`・`link_url`は埋まっている行が複数あるのに、`image_url`列はほぼ全滅していた。

### 原因は3つ重なっていた

**① Mixed Content（https上でhttp画像を読み込めない）**
Google Books APIの`imageLinks.thumbnail`が`http://`スキーマで返ることがあり、Vercel（https）上でそのまま`<img src>`に入れるとブラウザに静かにブロックされる。
→ 対策：`fetchBookMeta_`/`fetchMusicMeta_`/`fetchPodcastMeta_`内で取得した画像URLを`.replace(/^http:\/\//, 'https://')`する処理を追加済み。

**② `syncWorksSheet()`のAPI呼び出し条件が厳しすぎた（最大の原因）**
旧コードは「creator・image_url・link_urlの**全部**が空」の時だけAPIを呼んでいた。ショーノートの①リンク記法（`📚[タイトル](URL)著者`）で著者・URLを直接指定した本は、その時点でcreator/link_urlが埋まってしまうため、**画像取得APIが永久にスキップされる**構造になっていた。
→ 対策：条件を「image_urlだけが空」に変更。creator/link_urlは①リンク記法や既存シート値を尊重し上書きしないよう調整済み（`syncWorksSheet`内、`rowFor`のAPI呼び出しブロック）。

**③ `+`記号がencodeURIComponentで壊れていた**
著者ヒントをGoogle Books検索クエリに足す際、`'intitle:' + title + '+inauthor:' + authorHint`のように**リテラルの「+」文字**で連結していた。この文字列全体を`encodeURIComponent`にかけると「+」が`%2B`に変換され、Google側でAND区切りとして認識されなくなり、著者ヒント付きの検索が軒並み失敗していた。
→ 対策：`'+inauthor:'`を`' inauthor:'`（半角スペース）に変更。スペースなら`%20`にエンコードされ正しく機能する。

### 副次的な改善
- `fetchBookMeta_`/`fetchMusicMeta_`/`fetchPodcastMeta_`すべてに第2引数（著者/アーティストのヒント）を追加。ショーノートの①リンク記法や既存シート値から拾った`creator`を渡すことで検索精度が向上
- `fetchBookMeta_`は「著者ヒントあり＋日本語優先→日本語制限なし→ゆるい全文検索」の3段階フォールバックに変更（マイナー本の取りこぼし対策）

### 【重要・未解決】Google Books APIの1日クォータ切れ（429）
デバッグ中、Google Books API（未認証・キーなし）の1日あたりの利用上限に達し、`status=429`が返る状態になった。原因は上記②③の修正前に複数回連続実行してしまったこと。

- **翌日（日付が変わった後）に再実行すればクォータはリセットされる見込み**
- 再発防止として、`tryQuery_`が429を検知した場合は3段階のフォールバックを即座に諦めるよう修正済み（`QuotaError_`という専用エラー型で判定し、無駄なリクエストを打たないようにした）。1冊あたり最大3リクエスト消費していたのを抑制できるはず
- **次チャットの最初にやること**：スプレッドシートのメニュー「作品シート」→「最新の内容に更新する（API取得あり）」を実行し、`image_url`列が埋まるか確認する。`API_LIMIT = 30`の上限があるため、`inspired`シートの規模次第では複数回の実行が必要になる可能性がある

### デバッグ用に追加した一時コードについて
調査のため`Logger.log('DEBUG ...')`（`syncWorksSheet`内）・`Logger.log('BOOKS_HTTP status=...')`・`Logger.log('ERROR: ...')`（`tryQuery_`内）を追加した。**現状のコードにまだ残っている**。動作確認が完全に終わったら削除して問題ない（実害はないが、次チャットで気になったら整理してよい）。

---

## 2. 概念詳細ページ（`concept.html`）の関係性拡張（本番反映済み）

### 追加した4項目（`concepts`シートの新しい手動列）

| 列名 | 役割 | 入力形式 | サイトでの見え方 |
|---|---|---|---|
| `proposer`（提唱者） | その概念を最初に言い出した人 | 自由記述（例：イノウエ） | 見出しカードにバッジ表示 |
| `related_concepts`（近接概念・自分たちで話した分） | 番組内で語った、意味が近い別の概念 | **概念名**を「、」「／」「/」のいずれかで区切って複数可 | クリックできるチップ（そのページへ遷移）。**片方の概念にだけ書けば、書いた相手側のページにも自動で逆リンクが出る（双方向）** |
| `related_external`（近接概念・文献由来） | 番組では話していないが、別の本や記事などで見かけた近い概念・引用 | 自由記述。複数件あれば**セル内で改行（Alt+Enter／Cmd+Enter）して行を分ける**。句読点・スラッシュ・カンマは文中にそのまま使ってOK | クリックできないテキストのチップ（リンクなし）。書いた側のページにだけ表示 |
| `contrast_concepts`（対比概念） | 意味が逆・対になる概念 | **概念名**を区切って複数可（`related_concepts`と同じ区切りルール） | クリックできるチップ。`related_concepts`と同じく双方向で自動反映 |

### 設計上のポイント
- `related_concepts`と`contrast_concepts`は「概念名」を指定する方式なので、既存の概念名リストと完全一致した場合のみリンクとして機能する（`buildConceptsData()`内で`nameSet`と照合し、存在しない名前は静かに無視する設計）。**双方向表示のロジックもここで実装**：片方の行にだけ書かれていても、GAS側（`buildConceptsData()`）で自動的に相手側にも逆リンクを追加している
- `related_external`だけ区切り方式が異なる（改行区切り）。理由は、引用文をそのまま書きたいという要望があり、「、」「／」「/」は引用文中に普通に登場しうるため区切り文字として使えなかったため。`splitByLine_()`という専用の分割関数を新設して対応
- フロントエンド（`concept.js`）では、`related`（自動双方向・リンクあり）と`related_external`（自由記述・リンクなし）を同じ「近接概念」セクション内でサブ見出しを分けて表示。両方空の時だけ「まだありません」を表示

### 変更したファイル（すべて反映済み）
- `gas/Code.gs`：`CONCEPT_HEADERS`定数、`CONCEPT_MANUAL_COLS`定数、`readConceptManual_()`、`splitConceptList_()`（新設）、`splitByLine_()`（新設）、`buildConceptsData()`、`syncConceptsMasterSheet_()`内`rowFor`
- `concept.html`：見出しカードに`proposer`バッジ用の要素、「近接概念」「対比概念」の新セクションを追加
- `concept.css`：`.head-meta`、`.proposer-badge`、`.chip-group`、`.chip-group-label`、`.contrast-chip`、`.external-chip`を新設
- `concept.js`：`makeLinkChip()`ヘルパー新設、`renderPage()`に提唱者・近接概念（2種）・対比概念の描画処理を追加

### 未確認・次チャットでやってもよいこと
- 実データでの表示崩れ確認（特に`related_external`に長文の引用を複数入れた時の見た目）は、今回1件のスクリーンショット確認のみ。他の概念でも試すと安心
- `contrast_concepts`（対比概念）は今回まだ実データで試していない（`related_concepts`と`related_external`は確認済み）。次チャットの最初に一度試すとよい

---

## 3. ショーノート入力ルールのドキュメント更新

`SHOWNOTE_RULES.md`（GitHub）に、以下のセクションを追記する想定（ユーザー側で追記済みかは次チャットで要確認）。

```markdown
## 概念の詳細情報（concepts シートに直接入力）

以下の4つは、ショーノート本文ではなく、管理シートの `concepts` シートに直接入力する
（`#タグ`はショーノートから自動抽出されるが、この4項目は自動計算できない情報のため手入力）。

| 列名 | 役割 | 入力形式 | サイトでの見え方 |
|---|---|---|---|
| `proposer`（提唱者） | 最初に言い出した人 | 自由記述（例：イノウエ） | 見出しカードにバッジ表示 |
| `related_concepts`（近接概念・自分たちで話した分） | 番組内で語った近い概念 | 概念名を「、」「／」「/」区切りで複数可 | ページリンクチップ・双方向自動表示 |
| `related_external`（近接概念・文献由来） | 文献・引用由来の近い概念 | 自由記述。複数はセル内改行（Alt+Enter）で区切る | テキストのみのチップ（リンクなし） |
| `contrast_concepts`（対比概念） | 対になる概念 | 概念名を区切って複数可 | ページリンクチップ・双方向自動表示 |

**注意点**
- `related_concepts`と`contrast_concepts`は、既存の概念名と完全一致している必要がある（タイプミスがあるとリンクが繋がらない）
- `related_external`は自由記述なので正式なタイトル名でなくてもよい
```

---

## 4. ルールの共有方法（完了）

### 課題
ショーノート・概念関連の書き方ルール（`#`/`##`タグ、`📚🎬📺🎵📻`の3段階記法、`concepts`シートの手動列4つ等）が積み重なり、煩雑になってきた。これは相方のカワサキさんも実際に書く際に必要なルールであり、共有方法を検討したい、という要望があった。

### 決定した方針
`SHOWNOTE_RULES.md`（GitHub）は開発者向けの正式仕様書として残しつつ、**実際にカワサキさんが手を動かす場所＝スプレッドシート内に、同じ内容の「早見表」タブを新設する**。GitHubの`SHOWNOTE_RULES.md`は正式仕様、スプレッドシートのタブは現場用の実用早見表、という役割分担にした（二重管理にはなるが負担は小さい）。

### やったこと（本チャットで完了）
- 「書き方ルール」タブに貼り付けるための全文テキストを作成・提供済み（概念タグ`#`/`##`のルール、作品マーカー`📚🎬📺🎵📻`の3段階記法、`concepts`シートの手動列7つ＝`description`/`reading`/`proposer`/`related_concepts`/`related_external`/`contrast_concepts`/`memo`の役割、困った時のFAQを含む）
- Googleスプレッドシートは改行入りテキストを貼り付けると自動的に1行ずつセルに分かれる仕様のため、コピペのみで完了する形で用意した
- `shownote`列見出しへの「メモを挿入」の手順も案内済み

### 次チャットで確認すること
- ユーザー側で実際に「書き方ルール」タブの貼り付け・`shownote`列へのメモ挿入が完了しているか確認する（本チャット終了時点では手順を渡した段階）
- 可能であれば、`#タグ`・`📚`等マーカーの案内を「概念一覧」「INSPIRED一覧」ページ内にも軽く出す案は、今回は着手せず（アイデア段階のまま持ち越し）

---

## 5. 技術的な注意点まとめ（前回までの引き継ぎ書から継続、一部更新）

- **GASのデプロイは「デプロイを管理」→既存デプロイの鉛筆アイコン→新バージョン**で行う。「新しいデプロイ」を選ぶとURLが変わるバグになる
- **GASレスポンスは10分間キャッシュされる**（`CacheService`、`HANDOVER_後片付け`フェーズで導入）。反映確認は10分待つこと
- **【新規】Google Books API（未認証）には1日あたりのクォータがある**。429エラーが出たら翌日まで待つか、クォータ消費を抑える実装（429検知で即座に諦める）を活用する
- **【新規】UrlFetchAppでのクエリ文字列連結時、リテラルの「+」記号は`encodeURIComponent`で`%2B`に変換され、URLクエリのスペース代わりとして機能しなくなる。** 単語区切りには半角スペースを使うこと
- **【新規】外部API（Google Books/iTunes）から取得した画像URLは、httpのまま保存するとVercel（https）でMixed Content扱いされ表示されない。** 必ず`.replace(/^http:\/\//, 'https://')`で正規化してから保存する
- **メニュー実行（概念シート／作品シート→最新の内容に更新する）とWebアプリの再デプロイは別物**。シートの中身を最新化したいだけならメニュー実行のみでよい
- **絵文字マーカーは文字クラス`[ ]`でまとめない**。必ず交互パターン`( | )`で書く
- **GAS内で日付型セルを判定する時は`instanceof Date`を使わない**。`Object.prototype.toString.call(value) === '[object Date]'`を使う
- **`#`の個数判定は正規表現`/#+([^\s#、。！？…「」『』【】（）]+)/g`でマッチさせたあと、マッチ文字列先頭の`#`の個数を数えて判定する**方式で統一されている
- **非公開リポジトリなのでraw GitHub URLは取得不可。** ファイルの中身を見るにはユーザーに貼ってもらう（またはVercelの公開URL経由で直接確認する）
- **mainブランチは削除済み。GASパスワードは別シートに移管済み**（過去チャットから継続）
- テスト：リポジトリ直下で `npm install && npm test`
- ローカル確認：`python3 -m http.server 8000` → `http://localhost:8000/episodes.html?src=mock.json`

---

## 6. ファイル構成まとめ（リポジトリルート直下・`feature/logo-deco`ブランチ）

今回のチャットで変更されたファイルは以下の4点：

| ファイル | 役割 | 今回の変更 |
|---|---|---|
| `gas/Code.gs` | データ配信・シート管理 | 画像取得API修正（3箇所）、概念の関係性フィールド追加（4箇所）。デバッグ用ログが一部残存 |
| `concept.html` | 概念詳細ページ | 提唱者バッジ・近接概念・対比概念セクションを追加 |
| `concept.css` | 概念詳細ページのスタイル | 上記に伴うクラス追加 |
| `concept.js` | 概念詳細ページのロジック | 提唱者・近接概念（2種）・対比概念の描画処理を追加 |

その他のファイル（`index.html`、`episodes.*`、`concepts.*`、`inspired.*`、`contact.html`、`nav.js`、`logo.js`、`page.css`、`about.html`、`log.html`）は変更なし。

---

## 7. ユーザーとのコミュニケーション上の注意（継続）

- **Git CLIは使わない。** GitHubのWeb画面（ドラッグ&ドロップ・鉛筆アイコン編集）で全て行う
- 「ファイル形式で吐き出してほしい」という要望が継続しており、コード変更は差分より**ファイルまるごと生成**が引き続き機能している
- 今回のデバッグは「エラーを握りつぶすtry/catch」が原因究明を難しくした一例だった。今後同種の不具合調査では、**まず`Logger.log`でエラー内容やHTTPステータスを可視化してから原因を特定する**という進め方がスムーズだった。次チャットでも同じ調査手順（ログ仕込み→メニュー実行→「実行数」画面でログ確認）が使える
- スクリーンショットを見ながら1ステップずつ進める。一度に複数手順を投げない
- 「今日はここで完了にする」といった区切りの意思表示があった場合は、素直に引き継ぎ書の作成に入ってよい
- 相方のカワサキさんとルールを共有する必要性が明確に意識されるようになっている。次チャット以降、カワサキさん向けの分かりやすさ（専門用語を避けた説明、スプレッドシート内で完結する導線）を一段階意識するとよい
