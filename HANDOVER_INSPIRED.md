# 引き継ぎ書：ラッキーもくもくチャンス — INSPIRED（作品一覧）フェーズ

作成日: 2026-07-15
前回の引き継ぎ書: `20260715_HANDOVER_CONTEXTS.md`（2026-07-14）

---

## 0. このチャットで何をしたか（サマリー）

大きく3つの作業を行った回。
1. **Vercelの`main`ブランチ問題を根本解決**（Production Branchの向き先を修正）
2. **CONTACTページの新規作成・本番反映**（Googleフォーム連携）
3. **INSPIREDページ（本・映画・アニメ/ドラマ・音楽・ラジオ/Podcastの一覧）を新規設計・実装**。ショーノートへの書き方から、GASでの自動抽出、フロントの一覧ページ、既存作品への著者/リンク情報の埋め込みまで一通り完了

### 到達点
- ✅ Vercel「Settings → Environments → Production → Branch Tracking」を`main`→`feature/logo-deco`に修正（これが「PUSHしても本番に反映されない」問題の直接原因だった）
- ✅ Googleフォーム作成・公開・メール通知設定、`contact.html`作成・本番反映（nav.jsの「CONTACT」項目が404だったのが解消）
- ✅ GAS改修：`inspired`シート新設、ショーノートから`📚🎬📺🎵📻`マーカーを抽出する仕組み、Google Books/iTunes APIによる半自動取得、Markdownリンク記法によるショーノート直接指定に対応
- ✅ `inspired.html` / `inspired.css` / `inspired.js` を新規作成・本番反映
- ✅ `nav.js`に7項目目「INSPIRED」を追加（CONCEPTSとLOGの間）
- ✅ 既存の作品十数件について、著者名・リンクをWeb検索で調査し、シートに埋め込み済み

---

## 1. Vercel「PUSHしても反映されない」問題の解決（最優先で対応・完了）

### 原因
過去チャットで発生した「main / feature-logo-deco ブランチのねじれ」問題を調査する過程で、根本原因が判明した。

- Vercelの「Production」環境が、ずっと**`main`ブランチ**を追跡する設定のままだった
- 実際の開発は毎回`feature/logo-deco`にpushしていたため、Production環境に自動反映されず、**毎回手動で「Promote to Production」が必要**になっていた

### 対応
1. GitHubの `compare/main...feature/logo-deco` を確認 → mainには2つの独自コミットがあったが、両方とも既に`feature/logo-deco`側に別の形で取り込み済み（GAS_URL更新／`[img:key]`画像埋め込み機能）と確認。**mainにしか無い変更は無い**ことを確認済み
2. Vercel「Settings → Environments → Production」の「Branch Tracking」を`main`→`feature/logo-deco`に変更・保存

### 現状・申し送り
- これで今後は`feature/logo-deco`へのpushが自動的にProduction反映されるはず（次回以降のpushで動作確認するとよい）
- `main`ブランチ自体はまだリポジトリに残っている（実害は無いので急ぎではないが、そのうち削除してよい）

---

## 2. CONTACTページ（本番反映済み）

### やったこと
1. Googleフォームを新規作成（「ラジオネーム」「メールアドレス」「本文」の3項目、ラジオネームと本文は必須）
2. フォームを公開設定に変更（デフォルトで「非公開」だったため、最初は誰も回答できない状態だった）
3. 新しい回答のメール通知をON（`lucky.mokumoku.chance@gmail.com`宛て）
4. `contact.html`を新規作成：`about.html`と同じ`page.css`の「雲の中」デザインを踏襲。フォーム送信は隠しiframeへの裏送信方式（ページ遷移せず「お便りありがとうございました！」に切り替わる）
5. GitHubにアップロード・本番反映確認済み

### Googleフォームの情報（今後フォームを直接いじる時のために記録）
- 編集URL: `https://docs.google.com/forms/d/1zYslfvjJQwApfnpoQoYy21iDggmgCfyfiFcr-cgZdTY/edit`
- 送信先（`contact.html`のform action）: `https://docs.google.com/forms/d/e/1FAIpQLSf3bsrpPftzvITWi7wwMyhyFHFmH44mDJ6RSp9sGkXXKDCsOg/formResponse`
- entry ID：ラジオネーム=`entry.1990855270` / メールアドレス=`entry.1973934207` / 本文=`entry.662891078`

### 技術メモ
- `page.css`自体は変更していない（`about.html`への影響を避けるため）。`contact.html`側に`<style>`で専用スタイルを自己完結で追加（`concepts.js`のリンクスタイル注入と同じ考え方）
- `nav.js`は既にCONTACT項目を持っていたので、`nav.js`側の変更は不要だった

---

## 3. INSPIRED（作品一覧）ページ — 設計の核

### 背景・要望
「参考図書・アニメ・映画などの作品リスト集を作りたい。抜き出しもある程度自動化したい」という要望から着手。CONCEPTS（`#`概念タグ）と同じ「ショーノートが唯一の真実」という設計思想を踏襲しつつ、著者/監督などのメタ情報だけAIやAPIの力を借りる、という設計にした。

### カテゴリ（絵文字マーカー）
| 絵文字 | 種別（type値） | ラベル |
|---|---|---|
| 📚 | book | 本 |
| 🎬 | movie | 映画 |
| 📺 | anime | アニメ/ドラマ |
| 🎵 | music | 音楽 |
| 📻 | radio | ラジオ/ポッドキャスト |

「WORKS」という項目名候補は「自分たちの作品と誤解されそう」という指摘で却下し、「**INSPIRED**」（影響を受けた作品、のニュアンス）に決定。ファイル名・nav.jsのラベルもすべて`inspired`で統一している。

### ショーノートへの書き方（3段階・優先度順）— 最重要・要参照

このチャットの後半で、書き方を「タイトルだけ」から「タイトル＋URL＋著者を直接指定できる」形に拡張した。詳細は`SHOWNOTE_RULES.md`にまとめてあるが、要点は以下。

#### ① リンク記法（もっとも確実。新規追加した書き方）
```
📚[さむわんへるつ](https://example.com/book-page)ヤマノエイ
```
`[タイトル](URL)著者`の形。Markdownのリンク記法と同じ。著者は省略可（`[タイトル](URL)`のみでもOK）。
**ここに書いたタイトル・URL・著者は、シートの既存値やAPI取得結果より常に優先される**（＝毎回ここから上書き。唯一の真実はショーノート、という設計を踏襲）。

#### ② 囲み記法（タイトルにスペース・記号がある場合）
`『タイトル』` / `「タイトル」` / `[タイトル]` のいずれかでタイトルを囲む。URL・著者はここでは指定できない。

#### ③ 素の1語（一番手軽）
絵文字の直後にスペースなしでタイトルを書くだけ。スペース・読点・かっこ類が来た時点でタイトル終了と判定。

### creator（著者/監督等）・link_url・image_urlの決まり方（優先順位）

1. **①のリンク記法でショーノートに直接書かれていれば、それを最優先**
2. 無ければ、`inspired`シートに保存されている既存値（手動修正 or 過去のAPI取得結果）を温存
3. それも空欄なら、📚📚🎵📻（本・音楽・ラジオ）に限りAPIで自動取得を試みる（下記4章）
4. 🎬映画・📺アニメ/ドラマは**今のところAPI取得なし**（空欄のまま。将来TMDb連携で対応予定）

`image_url`（表紙/ジャケット画像）は**ショーノートには書けない仕様**（②③の書き方では取得できるが①のリンク記法でも画像URLは指定できない設計）。API自動取得 or シートへの直接書き込みのみ。

`description`（一言コメント）・`memo`（内部メモ）は完全手動。今回は「descriptionもAIに下書きさせる」案も検討したが、ユーザー判断で**見送り**、引き続き完全手動とすることで確定している。

---

## 4. GAS改修の内容（`Code.gs`・反映済み・要再デプロイ確認）

### 新しい定数・シート
- `SHEET_INSPIRED = 'inspired'`（当初`SHEET_WORKS = 'works'`だったが、シート名を「inspired」に統一するためリネーム済み。**この名前変更に合わせて、スプレッドシート側のシートタブ名も`works`→`inspired`に変更しておくこと**）
- `WORK_HEADERS`：`title` / `type` / `episode_count` / `episodes` / `episode_titles` / `exists` / `updated_at`（自動）+ `creator` / `image_url` / `link_url`（半自動）+ `description` / `memo`（手動）
- `WORK_EMOJI_TYPE` / `WORK_TYPE_LABEL`：絵文字⇔種別の対応表

### 新しい関数
- `computeWorks_()`：ショーノートから3段階の書き方（リンク記法／囲み記法／素の1語）をすべて拾う正規表現でマーカーを抽出。リンク記法があれば`inlineCreator`/`inlineLink`としてついでに回収する
- `readWorkAssistAndManual_()`：`inspired`シートの半自動列・手動列を`type|title`キーで読み込む
- `buildWorksData()`：`?type=works`エンドポイント用データ。上記の優先順位ロジックをここでも適用（サイト側はメニュー実行前でも常に最新のショーノート内容を反映する）
- `syncWorksSheet()`：【メニュー実行用】`inspired`シートを最新化。半自動列は「①ショーノート直接指定→②シート既存値温存→③API取得（book/music/radioのみ、1回の実行で最大30件）」の順で埋める
- `fetchBookMeta_(title)`：Google Books API（キー不要）
- `fetchMusicMeta_(title)`：iTunes Search API・`media=music`（キー不要）
- `fetchPodcastMeta_(title)`：iTunes Search API・`media=podcast`（キー不要）

### `doGet`分岐
`?type=works` を追加済み（`?type=concepts`等と同じ書き方）。

### メニュー
`onOpen()`に「作品シート → 最新の内容に更新する（API取得あり）」を追加済み（既存の「概念シート」メニューはそのまま）。初回実行時、外部サイトへの通信許可ダイアログが出るので「許可」する必要がある。

### 踏んだ罠（次チャットのAIが同じ轍を踏まないために）
- **絵文字を文字クラス`[📚🎬📺🎵📻]`でまとめて書くと内部的に壊れる**（絵文字はサロゲートペアのため、文字クラス中では正しくマッチしないことがある）。必ず交互パターン`(📚|🎬|📺|🎵|📻)`で書くこと。これで最初の実行時に`type`が空・タイトルが文字化けする不具合が発生し、修正した実績あり

---

## 5. フロントエンド（本番反映済み）

### `inspired.html` / `inspired.css` / `inspired.js`
- `concepts.html`と同じ構造・デザインパターンを踏襲（背景の敷き方、検索UI、カードグリッド）
- フィルターは「すべて／📚本／🎬映画／📺アニメ・ドラマ／🎵音楽／📻ラジオ・ポッドキャスト」
- カードには：サムネイル画像（無ければ絵文字プレースホルダー）／種別ピル／タイトル（`link_url`があればリンク化）／著者名／任意コメント／登場エピソード（開閉式）を表示
- データソースは`?type=works`の1本のみ（concepts.jsと同じ設計）

### `nav.js`
`PAGES`配列に`{ file: 'inspired.html', label: 'INSPIRED' }`をCONCEPTSとLOGの間に追加。開閉アニメーションの`transition-delay`も7項目分（6項目→7項目）に調整済み。

---

## 6. 既存データの充実（このチャットで手動投入）

ショーノートに書かれていた作品のうち、著者/監督情報が空欄だったものについて、Web検索で調査し`creator`・`link_url`の対応表を作成・ユーザーがシートに貼り付け済み（十数件、書籍・映画中心）。

### 未解決のまま持ち越したもの
- **「ハリー・ポッター」**：どの作品（8作品あり監督が異なる）を指すか特定できず、ユーザー確認待ちのまま次チャットに持ち越し。次チャットの最初に確認するとよい
- **「PERFECT DAYS」と「パーフェクト・デイズ」**：表記ゆれにより`inspired`シート上で別々の行（別作品）として存在している。同じ映画（ヴィム・ヴェンダース監督）なので、いずれどちらかの表記に統一したほうが集計上きれいになる（今すぐ対応不要）

---

## 7. 次チャットでやること（優先順位は未整理）

1. **「ハリー・ポッター」の作品特定**（上記6章）
2. **`PERFECT DAYS`表記統一の検討**（上記6章）
3. `concept_contexts`シートへの本格的な文脈書き込み作業（`HANDOVER_CONTEXTS.md`から持ち越し・継続中）
4. `graph.js`のタグノードクリック時の挙動変更（ツールチップ→`concept.html`遷移。複数回持ち越し・未着手）
5. `main`ブランチの削除（実害なし。Vercel設定修正で緊急度は下がったが、そのうち片付けたい）
6. 🎬映画・📺アニメ用のTMDb API連携検討（画像・監督情報の自動取得。ユーザーからは「まずは無料のGoogle Books/iTunesだけで」という方針で保留中）
7. GAS側のキャッシュ導入（`HANDOVER_CONTEXTS.md`から持ち越し・未着手）
8. スプレッドシートの見やすさ整備（自動列のグレー表示など、`HANDOVER_CONTEXTS.md`から持ち越し・未着手）

---

## 8. 技術的な注意点まとめ（前回までの引き継ぎ書から継続、一部更新）

- **GASのデプロイは「デプロイを管理」→既存デプロイの鉛筆アイコン→新バージョン**で行う。「新しいデプロイ」を選ぶとURLが変わるバグになる（発生実績あり）
- **メニュー実行（概念シート／作品シート→最新の内容に更新する）とWebアプリの再デプロイは別物**。シートの中身を最新化したいだけならメニュー実行のみでよく、`Code.gs`のロジック自体を変えたときだけ再デプロイが要る
- **絵文字マーカーは文字クラス`[ ]`でまとめない**。必ず交互パターン`( | )`で書く（本チャットで新たに判明した知見。5章参照）
- **GAS内で日付型セルを判定する時は`instanceof Date`を使わない**。`Object.prototype.toString.call(value) === '[object Date]'`を使う
- **`#`の個数判定は正規表現`/#+([^\s#、。！？…「」『』【】（）]+)/g`でマッチさせたあと、マッチ文字列先頭の`#`の個数を数えて判定する**方式で統一されている
- **非公開リポジトリなのでraw GitHub URLは取得不可。** ファイルの中身を見るにはユーザーに貼ってもらう（またはVercelの公開URL経由で直接確認する）
- **コミットしても反映されない時の最速の切り分け**：GitHubのコミット詳細ページ下部にVercelのステータスが付いているか確認する。**ただし今回Vercelの Production Branch 設定を修正したので、今後は基本的に自動反映されるはず**
- **画像URL（image_url）の著作権**：Google Books/iTunesのAPIが返す画像URLは表示用途での使用が許可されているため使用OK。一方、映画・アニメのポスター画像を検索結果から手動で拾ってきて貼るのはホットリンクになり著作権的にグレーなため、**現状は意図的に空欄のまま**にしている（TMDb API導入まで保留）
- **スプレッドシートにパスワードが平文で記載されている件**：過去チャットから継続の申し送り。削除・変更を推奨済みだが、今回のチャットでは状況を再確認していない。次チャットでも状況を尋ねてよい
- テスト：リポジトリ直下で `npm install && npm test`
- ローカル確認：`python3 -m http.server 8000` → `http://localhost:8000/episodes.html?src=mock.json`。`inspired.html?src=mock.json`でも同様にモックJSON差し替えで確認可能（未検証・次チャットで試してもよい）

---

## 9. ファイル構成まとめ（リポジトリルート直下・`feature/logo-deco`ブランチ）

| ファイル | 役割 | 今回の変更 |
|---|---|---|
| `index.html` / `graph.js` | グラフ（トップページ） | 変更なし |
| `episodes.html` / `episodes.css` / `episodes.js` | エピソード一覧＆検索 | 変更なし |
| `concepts.html` / `concepts.css` / `concepts.js` | 概念一覧 | 変更なし |
| `concept.html` / `concept.css` / `concept.js` | 概念詳細ページ | 変更なし |
| `inspired.html` / `inspired.css` / `inspired.js` | **作品一覧（新規）** | 新規作成・本番反映済み |
| `contact.html` | **お便りフォーム（新規）** | 新規作成・本番反映済み |
| `nav.js` | 共通ナビゲーション | INSPIRED項目を追加（7項目に） |
| `logo.js` / `page.css` | 共通ロゴ／ABOUT・CONTACT共通スタイル | 変更なし |
| `about.html` / `log.html` | 変更なし | - |
| `gas/Code.gs` | データ配信・シート管理 | `inspired`シート関連の機能を全面追加 |
| `SHOWNOTE_RULES.md` | **ショーノート入力ルール（新規）** | 新規作成（`#`概念タグ・作品マーカー両方を網羅） |

---

## 10. ユーザーとのコミュニケーション上の注意（継続）

- **Git CLIは使わない。** GitHubのWeb画面（ドラッグ&ドロップ・鉛筆アイコン編集）で全て行う
- スクリーンショットを見ながら1ステップずつ進める。一度に複数手順を投げない
- ユーザーは自己診断力・技術的な勘所がさらに上がってきており、Vercelの環境設定（Environments）まわりも自力でたどり着けるようになっている。ただし基本姿勢は初心者向けのまま、GitHub操作やGASの手順は画面操作レベルで具体的に説明を続けること
- 「AIに任せられる部分は任せたい」という志向が強まっている（著者情報の自動取得、Web検索での情報補完など）。ただし**ハルシネーションのリスクは明確に説明した上で**、確度の低いものは確認を挟む、という進め方が機能している（今回の「PERFECT」→「PERFECT DAYS」の確認や、ハリー・ポッターの特定保留がその例）
- 「まず動くものを見せて、見ながら微調整する」進め方が一貫して機能している
