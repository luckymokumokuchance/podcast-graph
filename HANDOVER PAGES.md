# 引き継ぎ書：ラッキーもくもくチャンス — ページ拡充フェーズ

作成日: 2026-07-11
前回の引き継ぎ書: `HANDOVER_VERCEL.md`（2026-07-10）

---

## 0. このチャットで何をしたか（サマリー）

`HANDOVER_VERCEL.md`で決定した「GitHub + Vercel移行」を実際に進めた回。**Vercelでの動作確認、共通コンポーネント（ナビ・ロゴ）の新設、ABOUTページの作成、背景の不具合修正**までを行った。

### 到達点
- ✅ Vercelにデプロイ成功。GitHub Pagesと並行稼働中（後述、まだ「本番」ではない）
- ✅ 全ページ共通のナビゲーション（`nav.js`）を新設し、index/episodes/log/aboutに導入
- ✅ 全ページ共通のロゴ（`logo.js`）を新設し、about/episodes/logに導入
- ✅ `about.html`を新規作成（Studio版のデザインを再現）
- ✅ スマホでスクロール時に背景が拡大/移動するバグを発見・修正
- ⚠️ 未解決：`main`ブランチと`feature/logo-deco`ブランチの構造的なねじれ（後述、要対応）

---

## 1. 現在のサイト構成

### URL
| 役割 | URL | 備考 |
|---|---|---|
| **今も実際の本番**（一般公開中） | https://lucky-mokumoku-chance.studio.site/1-1 | Studio。**まだ何も変更していない。触らない** |
| Vercel（作業・検証用） | https://podcast-graph-chi.vercel.app | 今回主に作業した場所 |
| GitHub Pages（並行稼働） | https://luckymokumokuchance.github.io/podcast-graph/ | Vercelと同じ内容が出る。反映がVercelより遅い（後述） |

**重要**: Vercel/GitHub Pagesは「次の本番候補」であり、まだ一般の視聴者には見えていない。独自ドメインをどちらに向けるかは未決定（`HANDOVER_VERCEL.md`から持ち越し、今回のチャットでは触れていない）。

### GitHubリポジトリ
`luckymokumokuchance/podcast-graph`（非公開）／作業ブランチ `feature/logo-deco`

Vercel・GitHub Pagesとも、このブランチをそのまま配信している（＝作業する場所は常にGitHubリポジトリ1箇所。「Vercelで作業する」という概念は無い）。

---

## 2. 新設した共通コンポーネント（重要）

今後ページを増やすときは、この2つを使い回す。

### `nav.js`（ページ間移動メニュー）
- 右下の丸ボタン。押すと**グレーの丸ノードが5つ**開く：`TOP / ABOUT / EP LIST / LOG / CONTACT`
- 今いるページの丸だけ緑になる（脈打ちアニメーションは無し。「今いる場所」の識別のみ）
- Studio時代のデザイン（右下の丸いメニュー）に寄せたリクエストに応えた形
- 自己完結・CSS込みの1ファイル。**ページを増やす時は`nav.js`内の`PAGES`配列に1行足すだけ**
- 5項目が画面に収まるよう、`100svh`基準でサイズ調整済み。スクロールバーが出ないようにも対応済み
- 導入済みページ：`index.html` / `episodes.html` / `log.html` / `about.html`
- ⚠️ CONTACTは`contact.html`が無いので現状404（次のタスクで解消）

### `logo.js`（左上の共通ロゴ）
- 3枚のロゴ画像（ラッキー／もくもく／チャンス）を左上に表示、クリックで`index.html`へ
- 横幅（`vw`）基準でサイズ計算。高さ基準（`vmin`）だと画面幅を超えてハミ出すバグを踏んだため
- **`index.html`（グラフページ）だけは導入していない**。理由：グラフページはD3.jsのcanvas内に`graph.js`が独自にロゴを描画する仕組み（`ASSET_BASE`パッチ済み）が既にあり、そちらが正のロゴ表示。`logo.js`と二重にならないよう意図的に外してある
- 導入済みページ：`about.html` / `episodes.html` / `log.html`

### `page.css`（ABOUT/CONTACT系ページ共通スタイル）
- 背景画像`bg_mokumoku.webp`（1440×1440、白い雲が焼き込まれた正方形画像）の中に、文字を確実に収める仕組み
- 白い雲の内側に収まる最大長方形を画像解析で測定済み：`x: 444〜1101 / y: 460〜923`（画像座標）、中心は画像中心から`(52.5, -28.5)`ズレている
- `--u`というCSS変数（`max(100vw,100vh)/1440`）で、画面サイズが変わっても雲の中に文字が収まるよう自動計算する
- 縦持ちスマホ用に、雲の中心へのズラしを一部無効化する分岐あり（横方向は画像が切れるため）
- **今は`about.html`専用に細かく調整した値が入っている**（「喫」の文字が雲にかぶらないよう下にずらす、等）。CONTACTページを作る際は同じ`page.css`を使い回せるが、コンテンツ量が変わるので配置の微調整が必要になる可能性が高い

---

## 3. このチャットで踏んだ罠と教訓（次チャットのAIが同じ轍を踏まないために）

### 罠1：`inset: 0` で背景を敷くと、スマホのアドレスバー開閉で背景が動いて見える
- `position: fixed; inset: 0;` は「今のブラウザ表示領域いっぱい」を毎回計算し直す
- スマホでスクロール→アドレスバーが引っ込む→表示領域が変わる→`background-size: cover`が再計算→背景が拡大/移動して見える
- **対策**：`height: 100lvh`（Largest Viewport Height）で高さを一度だけ確定させる。バーが出ている間は下端が画面外に少しはみ出すだけで、視覚的な破綻は起きない
- `episodes.css` / `log.css` / `page.css` の3ファイルとも修正済み。**今後CSSで背景を全画面に敷く時は、最初からこの書き方を使うこと**

### 罠2：Vercelの「Production Branch」設定はSettings内の場所がUIで変わっている
- 以前は Settings → Git 内にあったが、現在は **Settings → Environments → Production → Branch Tracking** に移動している
- ただし今回、実際には「Promote to Production」で個別デプロイを昇格させる形で解決した

### 罠3：ファイルを直したつもりでも、実際は古いバージョンのままサーバーに残っていることがある
- 一番確実な確認方法：**`https://podcast-graph-chi.vercel.app/ファイル名` を直接ブラウザで開いて中身を見る**
- スクリーンショットや体感の「変わらない」より、この確認が最速。今回`nav.js`の反映漏れをこれで特定できた
- 原因は恐らくPUSH漏れ（アップロード忘れ、または古いバージョンを誤って再アップロード）

### 罠4：GitHubの「Commit changes」ダイアログには2つのラジオボタンが隠れている
- 「Commit directly to the ブランチ名」と「Create a new branch and start a pull request」
- 下を選ぶと別ブランチに入ってしまい、何も反映されない。**常に上を選ぶこと**

### 罠5：Pull Requestは「Title」が空だと作れない（単純な入力漏れの可能性を先に疑う）

---

## 4. 未解決の技術的負債（優先度は次チャットで相談）

### `main`ブランチと`feature/logo-deco`ブランチのねじれ
- Vercelの「Production」ラベルは、一時`main`ブランチの古いコミット（`chore: update GAS_URL...`）に付いたままになっていた
- 「Promote to Production」で実害は解消したが、**根本のねじれ（2つのブランチの中身が食い違っている）はまだ残っている**
- 統合を試みたが、Pull Requestの段階で「Can't automatically merge」（コンフリクト）が発生し、今回は保留にした
- ページが増えるほど、このねじれを解消するコストも上がっていく。**次チャットで着手を検討すべき**

### GitHub Pagesの反映ラグ
- 同じコミットでも、VercelよりGitHub Pagesの方が反映が遅い・キャッシュが残りやすい実例を確認済み（相方さんへの説明材料に使える）

---

## 5. 次チャットでやりたいこと（ユーザーからの要望、優先順位は未整理）

1. **CONTACTページ作成**
   - Studio版のデザイン（スクショ確認済み：ラジオネーム / メールアドレス / 本文の3項目、送信ボタン）を`page.css`のクラウド枠内に再現する想定
   - 静的サイトはフォーム送信を自前で受け取れないため、**Googleフォームへの裏送信**が必要（LOG機能と同じGASパイプラインの流用を検討）
   - 着手前にGoogleフォームの新規作成が必要（項目：ラジオネーム・メールアドレス・本文）

2. **独自概念ノードのページ**
   - グラフのグレーノード（`#`のもの）をクリックして飛べる詳細ページ
   - 「どんな文脈で語られていたか」をAIで文字起こし/ショーノートから洗い出し、蓄積したい
   - データ設計（スプレッドシートへの追加カラム/新シート）を先に固める必要あり

3. **タグ化前の概念だけをまとめたページ**
   - 例：ジョグ型編集、本物っぽさ　など
   - 概念データに「ステータス：未タグ化／タグ化済み」フィールドを1つ足すだけで実現可能（`HANDOVER_VERCEL.md`で既に方針決定済み、別テーブル不要）

4. **本/映像/音楽などをまとめたページ**
   - ショーノートから特定の条件で自動抽出して掲載したい、という要望
   - 抽出条件（書式のルール等）を最初に決める必要あり

5. **各自のポートフォリオページ**（イノウエ・カワサキ）
   - 静的ページで十分という認識（`HANDOVER_VERCEL.md`から継続）

6. **ロゴの「緑のもくもく」デザインシステムの見直し**
   - 現状の背景画像（ラスター画像に白い雲が焼き込まれている形）はWebとの相性が悪いという課題感
   - 今回`page.css`で「画像解析→座標計算→CSS変数」という力技で対応したが、これは応急処置的な位置づけ
   - **別のデザインシステム**（CSSで雲の形自体を描く、SVGにする等）を検討したい、という声。上記の技術的な相性の悪さの実例（雲がラスター画像であるがゆえの面倒さ）は今回の`page.css`実装がそのまま説明材料になる

7. **トークテーマ掲載ページ**
   - Googleドキュメントに書いているトークテーマを、ほぼ自動でサイトに掲載したい
   - 既存のGAS Web Appパイプラインを拡張する形（Googleドキュメント→GAS→JSON配信）が候補。Google Docs APIの認証まわりの検討が必要

---

## 6. 技術的な注意点まとめ（前回までの引き継ぎ書から継続、変更なし）

- **GASのデプロイは「デプロイを管理」→既存デプロイの鉛筆アイコン→新バージョン**で行う。「新しいデプロイ」を選ぶとURLが変わるバグになる（発生実績あり）
- **GAS内で日付型セルを判定する時は `instanceof Date` を使わない。** `Object.prototype.toString.call(value) === '[object Date]'` を使う
- **GASの動作確認は `/exec` URLを毎回シークレットタブで開く**
- **非公開リポジトリなのでraw GitHub URLは取得不可。** ファイルの中身を見るにはユーザーに貼ってもらう（またはVercelの公開URL経由で直接確認する＝罠3参照）
- **スプレッドシートにパスワードが平文で記載されている**（削除・変更を推奨済み。今回のチャットでは再確認していない。状況を再度尋ねてよい）
- テスト：リポジトリ直下で `npm install && npm test`
- ローカル確認：`python3 -m http.server 8000` → `http://localhost:8000/episodes.html?src=mock.json`
- Webflowアカウント：Starterプランに登録済みだが検証不能と判明済み。放置でよい（`HANDOVER_VERCEL.md`から継続）

---

## 7. ユーザーとのコミュニケーション上の注意

- **Git CLIは使わない。** GitHubのWeb画面（ドラッグ&ドロップ・鉛筆アイコン編集）で全て行う
- **スクリーンショットを見ながら1ステップずつ進める。** 一度に複数手順を投げない
- ユーザーは技術的な勘所が掴めてきており、「反映されない時はURLを直接開いて確認する」等の自己診断ができるようになってきている。ただし基本姿勢は初心者向けのまま、具体的な画面操作レベルでの説明を継続すること
- 見た目の微調整（余白・サイズ・位置）は、数値を提示しつつ「もう少し」「これくらい」といった感覚的なフィードバックで素早く収束させるやり取りが機能している。往復を恐れず、まず動くものを見せて調整する進め方が合っている
- 相方さん（カワサキ）は「GitHub Pagesでもいいかも」と発言していたが、これは元々相方さんが問題視していた`?v=N`のキャッシュバスティング問題（Vercelなら不要）を解消する動機とズレている可能性がある。判断材料は今回のチャットで実例（GitHub Pagesの反映ラグ）が取れたので、共有する価値がある
- Claude in Chromeの接続は未使用のまま。接続されれば直接操作できる可能性あり
