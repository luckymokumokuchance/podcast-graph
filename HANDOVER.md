# 改修の引き継ぎメモ（2026-07-07）

## 今回やったこと

### 1. エピソード一覧＆検索ページを新設（`episodes.html`）
グラフと**同じGASのJSON**を読んで、一覧表示＋検索ができるページを作りました。

- タイトル・ショーノート本文・タグ・話数で即時検索（ひらがな/カタカナ/全角半角の違いを吸収）
- 検索語はハイライト表示
- #タグのチップをタップして絞り込み（複数選択でAND）
- ショーノート全文の開閉表示（Markdown・[img:key]画像対応。グラフのモーダルと同じ挙動）
- SpotifyのURLは自動で埋め込みプレイヤーに変換
- 新しい順に自動で並ぶ

**→ これにより、Studio CMSへのショーノート手入力は不要になります。**
スプレッドシートの `episodes` シートに1行足すだけで、グラフとこの一覧の両方に反映されます。

### 2. グラフに「見え方の調整」パネルを追加（`quickpanel.js`）
これまで調整スライダーはiframe埋め込み時（＝Studio上）には非表示でした。
新パネルは**サイト訪問者にも表示され**、右上の ☁ ボタンで開閉できます。

- せんの長さ / あつまり具合 / 円の大きさ / まわる速さ の4項目＋「もとに戻す」
- 設定はブラウザに保存され、次回訪問時も引き継がれます
- 既存の開発用パネル（PCで直接開いた時のみ表示）はそのまま残っています

### 3. GAS（`gas/Code.gs`）に公開日を追加
`episodes` シートに `published_at` 列があれば、一覧に日付が表示されます。
**列が無くても壊れません**（日付が出ないだけ）。

---

## 反映手順

### STEP 1: どのブランチが公開されているか確認
GitHubの Settings → Pages で、GitHub Pagesのデプロイ元ブランチを確認してください。
（公開中のグラフが v19 なので、おそらく `feature/logo-deco` です）

### STEP 2: ファイルをそのブランチに反映
新規・変更ファイルは以下です。GitHubのWeb画面から「Add file → Upload files」でまとめてアップロードできます。

- 新規: `episodes.html` `episodes.css` `episodes.js` `quickpanel.js` `mock.json` `package.json` `test_episodes.mjs` `test_quickpanel.mjs` `HANDOVER.md`
- 変更: `index.html` `graph.js` `gas/Code.gs` `.gitignore`

### STEP 3: GASを更新（任意・日付表示したい場合のみ）
1. スプレッドシートの「拡張機能 → Apps Script」を開く
2. `gas/Code.gs` の内容で置き換えて保存
3. 「デプロイ → デプロイを管理 → 編集(鉛筆) → バージョン: 新バージョン → デプロイ」
   ※URLが変わらない「デプロイを管理」からの更新がおすすめです
4. `episodes` シートに `published_at` 列を追加し、日付（例: 2026-03-31）を入力

### STEP 4: Studioに埋め込む
エピソード一覧ページ（現在Studio CMSで作っている一覧＋検索のセクション）を、
以下のiframe埋め込みに置き換えます。

```html
<iframe
  src="https://luckymokumokuchance.github.io/podcast-graph/episodes.html"
  width="100%"
  height="900px"
  style="border: none;"
></iframe>
```

動作確認できたら、Studio側の旧CMSリスト・検索セクションは削除してOKです。

---

## これからの更新フロー（1回入力で全部反映）

```
新エピソード公開
  ↓
episodes シートに1行追加（shownoteに #タグ も書く / WEB_status = published）
  ↓
（エピソード同士を手動で繋ぎたい場合のみ links シートに追加）
  ↓
おわり。グラフにも一覧・検索にも自動反映
```

---

## テストについて

`npm install` 後に `npm test` で、検索ロジックと調整パネルの自動テスト（計32件）が走ります。
手元で見た目を確認したい場合は、リポジトリのフォルダで簡易サーバーを立てて
`http://localhost:8000/episodes.html?src=mock.json` を開くとモックデータで確認できます。

```
python3 -m http.server 8000
```

---

## ⚠️ セキュリティに関する重要なお願い

管理用スプレッドシート（ラッキーもくもくチャンス_管理シート）に
**各アカウントのパスワードが平文で記載**されています。
リンク共有されているため、以下を強くおすすめします。

1. シートからパスワードの記載を削除する
2. 記載されていたパスワードは変更する
3. パスワードは専用のパスワードマネージャーで管理する

今回の改修作業にパスワードは一切使っていません。
