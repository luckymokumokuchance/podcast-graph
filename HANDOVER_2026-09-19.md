# 引き継ぎ 2026-09-19（イノウエ側Claude / Sonnet）

前回の引き継ぎ書: `HANDOVER_2026-08-21.md`
**このセッションでRSSをDBにする移行（Phase 1・2）を完了しました。** 着手前に必ずこれと `DESIGN_RSS_AS_DB.md` を読んでください。

---

## 0. いちばん大事なこと（3行）

1. **スプレッドシートへの記入がもう不要です。** Spotifyに公開するだけで本文・タグ・作品まで全部反映されます
2. **`app/shownotes.json` と `applyCurated()` は廃止しました。** 本文の正は常にRSS
3. RSSだけでは出てこない差分（古い回・後から足した概念）は `app/episodes-extra.json` に**追記**します。使い方は `EPISODE_WORKFLOW.md` 参照

---

## 1. 背景：ユーザーからの課題提起

> 理想:spotifyにあげたら自動判定およびHPに反映（RSSフィードをDBとして取り扱う）
> 現状:DBはスプレッドシート、spotifyにあげるが別作業となり負担がある

調査の結果、**理想はすでに半分成立していた**ことが判明した。RSS説明文28話中17話に、すでに`#タグ`が書かれていた。「Spotifyに上げたら反映される」は気づかれないまま部分的に動いていた。

## 2. Phase 1：表示・抽出の改善（低リスク、移行なし）

- **定型フッタの除去**（`stripBoilerplate`）：`●毎週火曜朝8時〜``✉️lucky.mokumoku.chance@gmail.com`等を、表示・抽出の前に除去。フッタは本文末尾とは限らず`[参考文献]`の前に挟まることがあるため、行単位で判定
- **作品抽出をHTML対応の二段構えに変更**：SpotifyはHTMLが書けるので`📚<a href="U">題</a>／著者`のようにリンクを書ける。まず`<a>`をHTMLのまま拾い（ここでしかURLは取れない）、残りをプレーンテキストとして処理する2段階に変更
  - 検証済み：RSSはCDATAでHTMLをそのまま運び、`marked`はHTMLを素通しするため、**HTMLが文字として見えることはない**（28話中16話に既に`<a href>`が入っており漏れゼロを確認）
- **著者の区切り方を修正**：`📚[題](URL)著者`のように区切り文字なしで著者が続く旧スプレッドシート形式と、`📚題／著者`のように区切り必須の新形式の両方に対応。`・`は区切りに使わない（「ゆる学徒カフェ・オープンマイクチャンネル」のように題名の一部であることが多いため）

### バグ修正（本番に実害あり）
URL内に`(`を含むWikipediaリンク（`_(映画)`形式）で、リンクが途中で切れ余った`)`が著者名として表示される不具合を発見・修正（ep20 Alian / ep22 羅生門 / ep24 Interstellar・アントマンで実害確認）。GAS本体（`gas/Code.gs`）には同じバグが残っている（`QUESTIONS.md`参照、優先度低）。

## 3. Phase 2：追記層への移行（ここで目的達成）

### 設計
```
RSS(Spotifyの説明文) が本文の正
        ↓ 重ねる（追記だけ）
app/episodes-extra.json … RSSだけでは出てこない差分
  addTags   … RSSに無い#タグ・##候補
  workLinks … RSSに無い/URLや著者が欠けている作品の補完 {url, creator}
  addImages … 本文中の画像 [img:key]（唯一、表示本文にも追記する。<img>に置換されるため）
```

`addTags`/`workLinks`は`ep._extraText`という**表示に混ぜない隠しフィールド**に積んで
`computeConcepts`/`computeWorks`にだけ見せる。`addImages`だけは例外的に`descHtml`/`descText`
本体に追記する（`[img:key]`は表示時に`<img>`タグへ置換されるため、余計な文字が見えない）。

### ⚠️ 重要な訂正（教訓として必ず読むこと）

設計時点で「差分は19概念のみ、作品URLの差分は0件」と見積もっていたが、**これは誤りだった**。
`app/data.js`の書き換え前に検証スクリプトを走らせてしまい、比較対象の「RSS側」が実は旧
`applyCurated`経由で既にスプレッドシート全文と混ざったものだった（自分自身と比較していた）。

`app/data.js`書き換え後に撮り直したところ、実際の差分は**20話・作品68件**だった。古い回
（1〜20話あたり）は`📚`のような絵文字マーカーが無く「著者『題名』」という別形式で書かれており、
**作品そのものがRSSから1件も抽出できない**ケースが多数あった。単なるURL欠落ではなく作品の
存在自体が消える問題だったため、`workLinks`を「URL追加」から「著者・URL・（無ければ題名のみ）を
丸ごと補える」設計に広げて対応した。

**教訓**：`app/data.js`を書き換える最中の検証は、書き換えが完全に終わってから撮り直すこと。

### 削除したもの
- `app/shownotes.json`、`app/stale-check.js`（陳腐化警告。前提ごと不要になった）
- `scripts/snapshot-shownotes.mjs`、`scripts/migrate-episodes-extra.mjs`（一度きりの移行用、役目終了）
- `.github/workflows/sync-shownotes.yml`

### 新設・変更したもの
- `app/episodes-extra.json`（追記層。20話ぶんのデータを移行済み）
- `.github/workflows/sync-curation-meta.yml`（`sync-shownotes.yml`の後継。概念/作品の手入力分＋画像マップのみ同期。`npm install`ステップを追加＝jsdomが必要なため）
- `scripts/snapshot-curation-meta.mjs`：shownotes.json依存を解消し、実際に`app/data.js`をjsdomで動かして計算するように全面書き換え。**自分自身の`app/episodes-extra.json`も加味**してから「まだ埋まっていない項目だけ」をworks-meta.jsonに書き出す（さもないと二重管理になりズレの元になる）
- `scripts/verify-parity.mjs`：`shownotes.json`前提の比較をやめ、「RSS全話を1件も落としていないか」「`episodes-extra.json`の追記が実際に反映されているか」を検証する形に書き換え
- `package.json`：`npm run snapshot`（shownotes用）を削除。`npm run snapshot:curation`は継続

---

## 4. 現在の全体像

| データ | 置き場所 | 更新方法 |
|---|---|---|
| エピソードの存在・音声・公開日・本文・タグ・作品 | RSS（Spotifyの説明文） | **Spotifyに公開するだけ・自動** |
| RSSだけでは出てこない差分（##候補・古い回の作品情報・本文中画像） | `app/episodes-extra.json` | 手動編集（Phase3で●から編集可能にする予定） |
| 概念の説明・提唱者・関連・表紙画像 | `concepts-meta.json` / `works-meta.json` | 30分おき自動同期 |
| エピソード間リンク / LOG / テーブル表示調整 | `links.json` / `logs.json` / `table-layout.json` | ●から直接編集（完成済み） |

**GASにまだ依存しているもの**：概念/作品の手入力分（`concepts`/`concept_contexts`/`inspired`シート）、`episodes.html`（Studio埋め込み）。

---

## 5. コマンド一覧

```
npm run snapshot:curation # 本番GAS → concepts-meta.json / works-meta.json / images.json
npm run verify             # app/data.jsがRSS全話を落としていないか・extrasが反映されているか
npm run verify:concepts    # 概念・作品のGASとの差分レポート（合否判定ではない。常にexit 0）
```
※ verify系はローカルに静的サーバーが要ります：`python -m http.server 8123`

---

## 6. 次にやること

1. **Phase 3：概念・作品・episodes-extraの編集画面**（`app/curation.html`）。`logpost.js`と同じ合言葉方式で。要件：31件を1画面のリストでその場編集、未入力バッジと絞り込み、近接/対比概念は既存概念からの選択式
2. **`episodes.html` / Studio埋め込みの扱いを決める**（GAS依存が残る唯一のページ）
3. `QUESTIONS.md`の残項目：`gas/Code.gs`の同じURL括弧バグの修正、GAS_URLの最新デプロイ一致確認、「参考番組」シートの用途

---

## 7. 運用ルール（継続）

- 本番公開ブランチは `feature/logo-deco`（mainは削除済み）
- **`app/` 配下を直す。ルート直下のHTMLは転送用の殻**
- **新エピソードはSpotifyに公開するだけ。** スプレッドシートへの記入は不要（`episodes`/`links`/`shownote`列はもう読まれていない）
- `app/links.json` / `app/logs.json` / `app/table-layout.json` / `app/episodes-extra.json` は管理画面や手動編集で更新される。ローカルで触る前に必ず最新をpull/curlし、上書きしない
- `gas/Code.gs` は実デプロイとズレやすい。触る前に最新コードを貼ってもらう
- **`app/data.js`を書き換えている最中は、その変更を前提にした検証をしない。** 書き換えが完全に終わってから検証スクリプトを撮り直す（このセッションでの実際の失敗例。6節参照）
- **commit/pushしたら同じタイミングでHANDOVERも更新する。** 相手はpullするまで見えない前提で運用する
