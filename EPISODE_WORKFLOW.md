# 新エピソードを出すときの手順（2026-08-22版）

本番を `app/` に切り替えたあとの、実際の運用手順。
「どこに何を書くか」で迷ったらこれを見る。

---

## 結論（先に3行）

1. **スプレッドシートは今まで通り使えます。** 廃止していません
2. 違うのは**エピソード間のリンクだけ**。これはスプレッドシートではなく、サイトの「●」ボタンから登録します
3. 書いた内容は**30分以内に自動でサイトに反映**されます。PCでコマンドを打つ必要はありません

---

## 新エピソードを出すときの手順

### STEP 1｜Spotifyに公開する
これだけで**数分後には星図に丸が出ます**（RSSから自動で拾うため）。

ただしこの時点では：
- 本文はSpotifyに書いた素の説明文
- タグ（グレーの丸）は出ない
- 概念ページ・INSPIREDページにも出ない

### STEP 2｜スプレッドシートの `episodes` シートに1行追加する
ここが**本番の本文**になります。書く列：

| 列 | 内容 | 必須 |
|---|---|---|
| `id` | 話数の数字（例：`24`）。**RSSのタイトル先頭の番号と必ず一致させる** | ◎ 必須 |
| `title` | タイトル（話数を除いた部分） | ◎ |
| `url` | `https://open.spotify.com/episode/...` の共有URL | ○ 推奨 |
| `shownote` | **本文。ここが全ての親**（後述） | ◎ |
| `WEB_status` | `published` と書く。**これが無いとサイトに出ません** | ◎ 必須 |
| `公開日` | 日付 | ○ |

> ⚠️ `id` がRSSの話数とズレると、本文がそのエピソードに結びつきません（星図には出るのに本文が素のまま、という状態になります）。

### STEP 3｜待つ（最大30分）
GitHub Actionsが30分おきに自動で取り込みます。急ぐときはGitHubの
**Actions タブ →「Sync shownotes」→ Run workflow** で即時実行できます。

### STEP 4｜（必要なら）エピソード同士をつなぐ
**ここだけスプレッドシートではありません。**
サイト右上の小さな「●」→ 合言葉 → 「新しいリンクを追加」から登録します。

> スプレッドシートの `links` シートは、もうサイトから読まれていません。

---

## ショーノートに書ける記法（全部ここが起点）

**ショーノートを書くだけで、タグも概念ページも作品ページも自動で付いてきます。**
個別に登録する作業はありません。

| 書き方 | どこに出るか |
|---|---|
| `#テクスチャ` | 星図にグレーの丸が出る＋概念ページで「タグ済み」 |
| `##ジスベリ` | 星図には出さない。概念ページで「未タグ」として管理 |
| `📚[料理の四面体](URL)玉村豊男` | INSPIREDページに本として並ぶ |
| `🎬` `📺` `🎵` `📻` | 同上（映画／アニメ・ドラマ／音楽／ラジオ） |
| `[img:ep024_1]` | 本文中に画像を埋め込む（`images` シートにキーとDriveのfileIdを登録） |
| `**太字**` `[文字](URL)` `- 箇条書き` | Markdownとして表示される |

**`#` と `##` の使い分け**：`##` は「まだ整理していない概念」。あとで `#` に変える（`#` を1個消す）だけで星図に昇格します。追加の作業は要りません。

---

## どこに何を書くか（一覧）

| 書きたいもの | 場所 | 反映 |
|---|---|---|
| エピソードの本文・タグ・作品 | スプレッドシート `episodes` の `shownote` | 30分以内・自動 |
| 本文中の画像 | スプレッドシート `images`（key / fileId） | 30分以内・自動 |
| 概念の説明・提唱者・近接/対比概念 | スプレッドシート `concepts` | 30分以内・自動 |
| 概念がどんな文脈で語られたか | スプレッドシート `concept_contexts` | 30分以内・自動 |
| 作品の表紙画像など | スプレッドシート `inspired` | 30分以内・自動 |
| **エピソード間のリンク** | **サイトの「●」→ リンク編集** | **約1分** |
| **LOG（日誌）** | **サイトの「●」→ LOG投稿** | **約1分** |
| テーブル表示の見た目調整 | サイトの「●」→ レイアウト | 約1分 |

---

## GASのメニューを手で実行する必要がある場面（2つだけ）

スプレッドシートを開いたときに出るメニューです。**新エピソードを出すだけなら不要**で、
以下をやりたいときだけ実行します。

1. **概念に説明文を書きたいとき**
   → メニュー「概念シート → 最新の内容に更新する」
   新しく出てきた概念の行がシートに作られるので、そこに説明を書く

2. **作品の表紙画像を自動取得したいとき**
   → メニュー「作品シート → 最新の内容に更新する（API取得あり）」
   新しい作品の行が作られ、本や音楽は表紙画像を自動で探しにいく

---

## 実装済み / これからやること

### ✅ 実装済み
- エピソードの存在・音声・公開日 … RSSから自動
- ショーノート・タグ・概念・作品 … スプレッドシート＋30分自動同期
- 概念ページ / 未タグページ / INSPIREDページ … `app/` へ移植済み
- エピソード間リンク … 「●」→ リンク編集
- LOG … 「●」→ LOG投稿（スプレッドシート・GASから完全に独立）
- テーブル表示調整 … 「●」→ レイアウト

### ⬜ これからやること

| やること | 今どう困るか | 優先度 |
|---|---|---|
| **概念・作品の編集画面**（Step 4） | スプレッドシートを開く必要がある。近接概念は手打ちで、打ち間違えると黙って無視される | 中 |
| **ショーノートの編集画面**（Step 5） | スプレッドシートを開く必要がある。**これができるとGASが完全に引退** | 中 |
| **`episodes.html` の扱い** | Studioにiframe埋め込み中で、ここだけ旧方式のまま。GAS依存が残る唯一のページ | 要相談 |

> ⚠️ Step 5（ショーノート編集画面）を作ったら、`.github/workflows/sync-shownotes.yml` は**必ず削除**すること。
> 直接編集と自動同期を併用すると、新しい編集を古いスプレッドシートの内容で上書きします。

---

## 困ったときの確認手順

**新しいエピソードの本文が反映されない**
1. `WEB_status` が `published` になっているか
2. `id` がRSSの話数と一致しているか
3. Actionsタブで「Sync shownotes」が成功しているか（失敗していれば赤く出ます）
4. 「●」から管理画面に入ると、ショーノートが未取得のときに警告バナーが出ます

**タグが星図に出ない**
- `#` が1個か確認（`##` は意図的に星図へ出さない仕様）
- タグの直後にスペースが必要（`#テクスチャです` のように続けると1語として拾われます）
