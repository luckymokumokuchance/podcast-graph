# 新エピソードを出すときの手順（2026-09-19版）

RSSをDBにする設計に移行したあとの、実際の運用手順。
「どこに何を書くか」で迷ったらこれを見る。

前回（2026-08-22版）からの最大の変化：**スプレッドシートへの記入が不要になりました。**

---

## 結論（先に3行）

1. **Spotifyに公開するだけで完成します。** スプレッドシートへの記入は不要になりました
2. `#タグ` も `📚作品` も、**Spotifyの説明文にそのまま書けば**星図・概念ページ・INSPIREDに反映されます
3. 昔ながらの「振り返って後から足す」「古い回の情報を補う」ときだけ、サイトの「●」から`app/episodes-extra.json`に追記します

---

## 新エピソードを出すときの手順

### STEP 1｜Spotifyに公開する
これで**すべて完成します**。RSSが自動で拾うので、数分〜十数分で反映されます。

タイトルの先頭には必ず話数を書いてください（例：`028 WEBサイト改造計画`）。
話数が無いとサイトに出ません。

### STEP 2｜説明文にタグ・作品を書く（Spotify側でOK）

| 書き方 | どこに出るか |
|---|---|
| `#テクスチャ` | 星図にグレーの丸が出る＋概念ページで「タグ済み」 |
| `##ジスベリ` | 星図には出さない。概念ページで「未タグ」として管理 |
| `📚料理の四面体／玉村豊男` | INSPIREDページに本として並ぶ |
| `📚<a href="URL">料理の四面体</a>／玉村豊男` | 同上＋リンク付き（Spotifyの装飾機能でリンクを貼ればOK） |
| `🎬` `📺` `🎵` `📻` | 同上（映画／アニメ・ドラマ／音楽／ラジオ） |
| `**太字**` `[文字](URL)` `- 箇条書き` | Markdownとして表示される |

**`#` と `##` の使い分け**：`##` は「まだ整理していない概念」。あとで `#` に変える（`#` を1個消す）だけで星図に昇格します。

以上で終わりです。**スプレッドシートは開きません。**

---

## それでも「●」から追記が必要なとき

Spotifyの説明文だけで表現しきれない、次の3つだけは`app/episodes-extra.json`への追記が要ります。
（現時点ではファイルを直接編集する必要があります。編集画面は今後作る予定）

| 追記したいもの | 使う場面 |
|---|---|
| `addTags` | 収録直後には無かった概念を、後から振り返って足したいとき |
| `workLinks` | 古い回（絵文字マーカーが無い回）の作品情報を補いたいとき |
| `addImages` | 本文中に画像（`[img:key]`）を埋め込みたいとき。`images`シートにkey/fileIdを登録した上で追記 |

```json
{
  "22": {
    "addTags": ["読書メモ", "##ジスベリ"],
    "workLinks": { "book|電気羊はアンドロイドの夢を見るか?": { "url": "https://...", "creator": "フィリップ・K・ディック" } },
    "addImages": ["ep022_1"]
  }
}
```

これは頻繁には使いません。**普段のエピソード追加では触らなくて大丈夫です。**

---

## エピソード間のリンク・LOG・テーブル調整（変更なし）

| やりたいこと | 場所 | 反映 |
|---|---|---|
| **エピソード間のリンク** | サイトの「●」→ リンク編集 | 約1分 |
| **LOG（日誌）** | サイトの「●」→ LOG投稿 | 約1分 |
| テーブル表示の見た目調整 | サイトの「●」→ レイアウト | 約1分 |

---

## スプレッドシートは今も使う場面がある（概念・作品の手入力）

以下はまだスプレッドシート側です。30分おきに自動でサイトへ同期されます。

| 書きたいもの | 場所 |
|---|---|
| 概念の説明・提唱者・近接/対比概念 | スプレッドシート `concepts` シート |
| 概念がどんな文脈で語られたか | スプレッドシート `concept_contexts` シート |
| 作品の表紙画像など | スプレッドシート `inspired` シート |

### GASのメニューを手で実行する場面（2つ）

1. **概念に説明文を書きたいとき** → メニュー「概念シート → 最新の内容に更新する」
2. **作品の表紙画像を自動取得したいとき** → メニュー「作品シート → 最新の内容に更新する（API取得あり）」

`episodes`/`links`/`shownote`列は**もう読まれていません**（記入しても反映されません）。

---

## 実装済み / これからやること

### ✅ 実装済み
- エピソードの存在・音声・公開日・本文・タグ・作品 … **RSS（Spotify）から自動、スプレッドシート不要**
- 概念の説明・表紙画像など … スプレッドシート＋30分自動同期
- 概念ページ / 未タグページ / INSPIREDページ … `app/` へ移植済み
- エピソード間リンク … 「●」→ リンク編集
- LOG … 「●」→ LOG投稿（スプレッドシート・GASから完全に独立）
- テーブル表示調整 … 「●」→ レイアウト

### ⬜ これからやること

| やること | 今どう困るか | 優先度 |
|---|---|---|
| **概念・作品・episodes-extraの編集画面**（Phase3） | スプレッドシートを開くか`episodes-extra.json`を直接編集する必要がある。近接概念は手打ちで、打ち間違えると黙って無視される | 中 |
| **`episodes.html` の扱い** | Studioにiframe埋め込み中で、ここだけ旧方式のまま。GAS依存が残る唯一のページ | 要相談 |

> ⚠️ Phase3で`app/`内に編集画面を作ったら、`.github/workflows/sync-curation-meta.yml` は**必ず削除**すること。
> 直接編集と自動同期を併用すると、新しい編集を古いスプレッドシートの内容で上書きします。

---

## 困ったときの確認手順

**新しいエピソードが星図に出ない**
- タイトルの先頭に話数（`028 タイトル`）が付いているか確認。付いていなければSpotify側で直す

**タグ・作品が反映されない**
1. RSSが更新されるまで数分〜十数分待つ（Spotify側の反映ラグ）
2. `#` が1個か確認（`##` は意図的に星図へ出さない仕様）
3. タグの直後にスペースが必要（`#テクスチャです` のように続けると1語として拾われます）
4. 作品名の直後の著者は `／` または `/` で区切る（`・` は区切りとして扱わない）

**古い回の作品にリンク・著者が付かない**
- Spotifyの説明文に絵文字マーカー（`📚`等）が無い古いスタイルの回は自動では拾えません。
  `app/episodes-extra.json` の `workLinks` に手で追記してください
