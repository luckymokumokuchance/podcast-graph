# 引き継ぎ 2026-07-23（Mac側Claude）

このセッションでやったこと・押さえておくべき運用ルールのまとめ。次にこのプロジェクトを触るセッション（Win側Claudeも含む）は、着手前にこれを読むこと。

## このセッションでやったこと

1. **レイアウト調整ページの新規作成**（`app/layout.html` / `layout.js` / `layout.css`）
   - `admin.html` と同じ合言葉ゲート＋暗号化トークンの仕組みを再利用。
   - テーブルビューのレイアウト定数をスライダーでライブプレビューし、「保存」で `app/table-layout.json` に GitHub Contents API 経由で直接PUTできる。
   - 発生したバグと修正：
     - **テーブルビューが見えない**：`#graph-container` がログイン前 `display:none` の中に入っていたため `clientWidth/clientHeight` が 0 で測定され、全ノードが原点に潰れていた。→ `#graph-container` は常に表示し、`#gate` だけを全画面 `position:fixed` オーバーレイに変更。
     - **`graph-ready` イベントの競合**：一度きりのイベントがログイン処理より先に発火することがあった。→ `graphReady`/`loggedIn` の2フラグ＋ `onBothReady()` で解決。

2. **本番サイトでタイトルが消える不具合の修正**（テーブルビュー、モバイルSafari限定）
   - 原因：`.row-title` に付けていた `overflow-x: auto` の横スクロールが、SVGの `<foreignObject>` 内でモバイルSafariだと描画自体が壊れる既知の不具合だった。
   - 対応：スクロールをやめ `overflow: visible` に変更。タイトルタップでモーダルの全文を読める形にした（→ 今回さらにタイトル自体に省略表示を再導入したので、モーダルの役割が再び重要）。

3. **「タイトルとタグの間隔」スライダーを追加**（`titleTagsGap`）

4. **テーブルビュー限定でショーノート冒頭プレビューを追加**（今回の本題）
   - タイトル下に、ショーノート（HTMLタグ除去済みの平文）の冒頭を表示。文字数はデフォルト200文字、`…` で省略。
   - `titleTagsGap` は「タイトル→ショーノートプレビューの間隔」に意味を変更（既存の保存値を活かすため、キー名はそのまま）。新規に `summaryTagsGap`（ショーノート→タグの間隔）を追加。
   - あわせてタイトルの文字の太さ（`titleWeight`）とタイトルの省略文字数（`titleTruncateLen`。これまでテーブルビューのタイトルは省略なしだったが、ショーノート行を追加したことで再度省略ありに戻した）も変数化。
   - ショーノートプレビューの文字サイズ（`summaryFont`）・文字数（`summaryLen`）・高さ（`summaryH`）も全部レイアウト調整ページのスライダーで変更できる。

## table-layout.json の現在のキー一覧（2026-07-23時点）

```
rowH, leftX, tableR, titleX, titleH,
titleFont, titleWeight, titleTruncateLen,
summaryFont, summaryLen, summaryH,
tagsFont, tagsGap,
titleTagsGap（タイトル→ショーノートの間隔）, summaryTagsGap（ショーノート→タグの間隔）
```

## 現在の運用ルール（重要・継続して守ること）

- **本番公開ブランチは `feature/logo-deco`**（`main` ではない）。
- **ルート直下の `index.html` / `js/graph.js` / `css/style.css` など（`app/` フォルダ外）は絶対に変更しない**。`app/` 配下のみ追加・変更する。
- **`app/links.json` と `app/table-layout.json` は、ユーザーが `admin.html` / `layout.html` の「保存」ボタンから GitHub Contents API へ直接PUTして更新できる**。ローカルで編集・デプロイする前に、必ず `curl` で本番の最新版を取得し、ユーザーが保存した値を絶対に上書きしないこと（今回のセッション中も、作業中に2回、ユーザーが `layout.html` から値を保存し直していた）。
- **デプロイ手順**：メインの作業ツリーには絶対に触れない（別マシン由来の未コミットの差分が残っているため、それらをコミット・破棄しない）。必ず以下の使い捨てworktreeパターンを使う。
  ```
  git worktree add --detach /tmp/lmc-deploy origin/feature/logo-deco
  # 該当ファイルだけコピー（links.jsonは除外。table-layout.jsonは直前にcurlで最新版を確認してからマージ）
  git add <対象ファイル> && git commit -m "..."
  git push origin HEAD:feature/logo-deco
  # pushがrejectされたら git fetch → 差分確認 → rebase（table-layout.jsonはコンフリクトしやすいので手動解決）
  rm -rf /tmp/lmc-deploy && git worktree prune
  ```
- 合言葉「mokumoku」で復号する共通のGitHub PAT（AES-GCM + PBKDF2、100,000回）を `admin.js` と `layout.js` で共有。新しい管理者機能を作る場合もこのパターンを再利用する。

## 未解決・要フォロー

- ショーノートプレビューを追加した分、行が縦に伸びる。現在の本番の「行の高さ」（`rowH`）のままだと次の行のタイトルと重なる可能性が高い。ユーザーに、`layout.html` の「行の高さ」「ショーノートの高さ」などのスライダーで実際に見ながら再調整してもらう必要がある（この場では見た目を確認できないため、深追いせず本人のスクリーンショット待ち）。
