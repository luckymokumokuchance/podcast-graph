# 質問・確認事項

HANDOVER*.mdほど大きくない、軽い質問・確認のやり取り用。
書いた人・答えた人がわかるように署名し、解決したら「解決済み」に変える。

---

## 未解決

### Q.【要対応・データ】ep020 のショーノート「Alian」のリンクが閉じカッコ不足で壊れている
- 現状：`🎬[Alian](https://ja.wikipedia.org/wiki/...._(%E6%98%A0%E7%94%BB)` と書かれており、**markdownリンクの閉じ `)` が1つ足りません**（URL内の `(映画)` は閉じているが、リンク記法自体が閉じていない）
- 影響：INSPIREDページで「Alian」にリンクが付きません（以前は途中で切れた壊れたURLが付いていた）
- 直し方：スプレッドシートの ep020 の shownote で、該当行の末尾に `)` を1つ足すだけ。30分以内に自動反映されます
- ついでに：作品名が `Alian` になっています（正しくは `Alien`）。直すならこの機会に
- 書いた人：Sonnet（2026-08-22）

### Q.【要対応・GAS】gas/Code.gs の作品抽出も URL内の `(` で壊れる（app/側は修正済み）
- 現状：`computeWorks_()` のURLパターンが `(https?:[^\s)]+)` で、Wikipediaの `_(映画)` 形式のURLだと途中で切れ、余った `)` が著者名として拾われます（ep20/22/24で実害を確認）
- app/側（`app/data.js` / `scripts/snapshot-curation-meta.mjs`）は 2026-08-22 に修正済み。**GAS本体は未修正**
- 暫定対応：`snapshot-curation-meta.mjs` の取り込み時に、`)` で始まる著者名と途中で切れたURLを除去しています。GAS本体を直せばこの正規化は不要になります
- 直し方：`gas/Code.gs` の該当箇所を以下に差し替えてGASエディタに貼り、「デプロイを管理 → 鉛筆 → 新バージョン」で再デプロイ
  ```
  旧: \\[([^\\]]+)\\]\\((https?:[^\\s)]+)\\)...
  新: \\[([^\\]]+)\\]\\((https?:(?:[^\\s()]|\\([^\\s()]*\\))+)\\)...
  ```
- 優先度：低（Step 5でGASが引退すれば自然に不要になるため）
- 書いた人：Sonnet（2026-08-22）

### Q. スプレッドシートの「参考番組」シートは何のため？
- 背景：episodes/links/images/logs以外に見つけたシート。用途がドキュメントに無い
- 聞きたい人：Win側Claude（河﨑）

### Q. graph.jsのGAS_URLは今のGAS最新デプロイと一致している？
- 背景：デプロイURL管理が「新バージョン」運用前提なので、念のため確認
- 聞きたい人：Win側Claude（河﨑）

---

## 解決済み

### Q. GASのlogs機能（buildLogsData / logsシート）、フロント側の対応はいつ頃の予定？
- 背景：GAS側に`buildLogsData()`が実装済みだが、`log.html`が実際にこのAPIを使っているか未確認だった
- 回答：`log.js`が`?type=logs`を叩いており、実際に動作していることを実データ（12件取得）で確認済み。フロント対応は完了していた
- 署名：Sonnet

### 訂正：「本番にも`##`混入バグがあるかも」という前回の指摘は誤りでした
- 背景：GASエディタの最新コードを直接貼ってもらい`gas/Code.gs`と突き合わせたところ、**リポジトリの`gas/Code.gs`が実際にデプロイされているコードより大幅に古い**ことが判明。前回「本番にも##バグがあるかも」と書いたのは、この古いリポジトリ版を見て判断したためで誤りだった
- 実際にデプロイ済みのコードでは、`buildPublicGraphData()`のタグ抽出は`#+`で連続数を数え`hashes !== 1`を除外する**正しいロジックが既に入っている**（`app/data.js`で今回入れた修正と同じ考え方）。本番のネットワークビューに`##`は混入していない
- 対応：`gas/Code.gs`をデプロイ済みの内容と完全に同期した（2026-08-06）
- 副次的な発見：デプロイ済みコードには、概念ページ（`concepts.html`）・作品ページ（`inspired.html`）向けのサーバー側計算（`buildConceptsData` / `buildWorksData`。共起計算、関連/対比概念の双方向リンク、Google Books/iTunes APIでの自動メタデータ取得など）が入っており、想定より大掛かりだった。この2ページを`app/`へ移植する際は、単純な静的化ではなく設計から要検討
- 教訓：**`gas/Code.gs`は「手動デプロイ」の性質上、リポジトリと実体がズレやすい。GAS側を触る作業の前には毎回最新コードを貼ってもらって同期確認すること**
- 署名：Sonnet（同期作業）、ユーザー（最新コード提供）

### Q. app/のRSS本文は、スプレッドシートのshownoteと別物（タグ・画像が失われる）
- 背景：`app/`移行の完全性検証中、実データ比較で判明。RSSの`<description>`とスプレッドシートの`shownote`列は同じ話数でも中身が違うテキスト（後者は公開後に人力で加筆・タグ追加・[img:key]埋め込みされたもの）。全21話中14話でタグ不一致、画像埋め込み`[img:key]`はRSS側に0/3
- 回答（Opus）：選択肢3（GitHub管理の静的ファイルに移す）を採用。データ取得コストは無視できる規模と判断
- 対応：`scripts/snapshot-shownotes.mjs`でGAS APIから`app/shownotes.json`（全21話のキュレーション本文）と`app/images.json`を生成。`app/data.js`の`applyCurated()`でRSS由来のエピソードにフィールド単位（descHtml/tags/link）で上書き。存在判定は常にRSS優先、本文はキュレーション優先＋無ければRSSにフォールバックする設計。`npm run snapshot`で今後も再同期可能
- 副次的な発見：ep9のスプレッドシート側urlが番組トップURL（入力ミス）だったため、`shownotes.json`のep9.urlを空にしてRSS側linkにフォールバックさせる形で回避（コード側でのガードはデータの誤りを隠すため不採用）
- 検証：`npm run verify`（`scripts/verify-parity.mjs`）で エピソード21件／タグ一致21/21／[img:]を含む回3/3／手動リンク10件／宣伝タグ非表示、すべてPASS確認済み
- 署名：Sonnet（実装・検証）、Opus（技術方針決定）
