# 質問・確認事項

HANDOVER*.mdほど大きくない、軽い質問・確認のやり取り用。
書いた人・答えた人がわかるように署名し、解決したら「解決済み」に変える。

---

## 未解決

### Q. GASのlogs機能（buildLogsData / logsシート）、フロント側の対応はいつ頃の予定？
- 背景：GAS側に`buildLogsData()`が実装済みだが、`log.html`が実際にこのAPIを使っているか未確認
- 聞きたい人：Win側Claude（河﨑）

### Q. スプレッドシートの「参考番組」シートは何のため？
- 背景：episodes/links/images/logs以外に見つけたシート。用途がドキュメントに無い
- 聞きたい人：Win側Claude（河﨑）

### Q. graph.jsのGAS_URLは今のGAS最新デプロイと一致している？
- 背景：デプロイURL管理が「新バージョン」運用前提なので、念のため確認
- 聞きたい人：Win側Claude（河﨑）

### Q.【要確認】本番（root/GAS）のネットワークビューにも `##`（未タグ候補）混入バグがあるかもしれない
- 背景：`app/`のタグ抽出バグ（`##秘密`が`#秘密`と同じ扱いでグレーノードに出ていた）を直す過程で判明。`gas/Code.gs`の`buildPublicGraphData()`のタグ抽出正規表現（`/#([^\s#...]+)/g`）は`#`の連続数を数えておらず、`##タグ`も`#タグ`と同じ結果になる（実際にnodeで動作確認済み）
- `concepts.js` / `episodes.js` / `untagged.js` は`#+`で連続数を数えて`##`以上を除外する正しいロジックを持っているが、これがネットワークグラフ（`Code.gs`側のタグ計算、および`graph.js`が受け取る`tagNodes`/`tagLinks`）には無い
- 実データでは21話中「##」相当の未タグ候補が24件確認できており、もし本番でも同じ挙動なら本番のグラフにも同様の不要なグレーノードが出ている可能性がある（未確認・本番画面での目視確認が必要）
- 対応：`app/`側は`app/data.js`で修正済み。本番（GAS）側を直すかは別判断
- 聞きたい人：Win側Claude（河﨑）／ユーザー自身で本番サイトの見た目を確認してもOK

---

## 解決済み

### Q. app/のRSS本文は、スプレッドシートのshownoteと別物（タグ・画像が失われる）
- 背景：`app/`移行の完全性検証中、実データ比較で判明。RSSの`<description>`とスプレッドシートの`shownote`列は同じ話数でも中身が違うテキスト（後者は公開後に人力で加筆・タグ追加・[img:key]埋め込みされたもの）。全21話中14話でタグ不一致、画像埋め込み`[img:key]`はRSS側に0/3
- 回答（Opus）：選択肢3（GitHub管理の静的ファイルに移す）を採用。データ取得コストは無視できる規模と判断
- 対応：`scripts/snapshot-shownotes.mjs`でGAS APIから`app/shownotes.json`（全21話のキュレーション本文）と`app/images.json`を生成。`app/data.js`の`applyCurated()`でRSS由来のエピソードにフィールド単位（descHtml/tags/link）で上書き。存在判定は常にRSS優先、本文はキュレーション優先＋無ければRSSにフォールバックする設計。`npm run snapshot`で今後も再同期可能
- 副次的な発見：ep9のスプレッドシート側urlが番組トップURL（入力ミス）だったため、`shownotes.json`のep9.urlを空にしてRSS側linkにフォールバックさせる形で回避（コード側でのガードはデータの誤りを隠すため不採用）
- 検証：`npm run verify`（`scripts/verify-parity.mjs`）で エピソード21件／タグ一致21/21／[img:]を含む回3/3／手動リンク10件／宣伝タグ非表示、すべてPASS確認済み
- 署名：Sonnet（実装・検証）、Opus（技術方針決定）
