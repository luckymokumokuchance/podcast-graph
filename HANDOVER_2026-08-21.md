# 引き継ぎ 2026-08-21（イノウエ側Claude / Sonnet）

前回の引き継ぎ書: `HANDOVER_2026-08-12.md`
**このセッションで本番を `app/` に切り替えました。** 河﨑さん側で作業する前に必ずこれを読んでください。

---

## 0. いちばん大事なこと（3行）

1. **ルート直下の8つのHTMLは、`app/` へ転送するだけの薄いファイルになりました。** 中身はもうありません
2. **今後ページを直すときは `app/` 側を直してください。** ルートを直しても誰も見ません
3. **スプレッドシートは30分おきに自動でJSONへ同期されます。** 手でコマンドを打つ必要はもうありません

---

## 1. 本番切り替え（このセッションの本題）

`scripts/make-redirects.mjs` で、ルート直下の8ページを `app/` の同名ページへ転送するHTMLに置き換えました。

| ルート | 転送先 |
|---|---|
| `/index.html` | `/app/index.html` |
| `/about.html` | `/app/about.html` |
| `/concepts.html` | `/app/concepts.html` |
| `/concept.html` | `/app/concept.html` |
| `/untagged.html` | `/app/untagged.html` |
| `/inspired.html` | `/app/inspired.html` |
| `/log.html` | `/app/log.html` |
| `/contact.html` | `/app/contact.html` |
| **`/episodes.html`** | **転送しない（後述）** |

### クエリ・ハッシュは引き継ぎます（重要）
`location.replace('app/xxx.html' + location.search + location.hash)` としています。
**Spotifyの各エピソード概要欄に `?ep=<id>` 付きのリンクが貼られているため**、ここを落とすと全部のリンクがただのトップページに着地します。実際に以下を検証済みです。

```
/index.html?ep=12  → app/index.html?ep=12                     PASS
/concept.html?c=テクスチャ(URLエンコード) → 引き継ぎOK          PASS
/inspired.html#top → app/inspired.html#top                    PASS
```

JSが無効な人向けに `meta refresh`（3秒）と手動リンクも入れています。

### episodes.html だけ転送していない理由
**Studioにiframeで埋め込まれているためです。** 勝手に別画面へ飛ばすと埋め込み先の見え方が壊れます。ここはGAS版のまま残っており、**まだGASに依存している唯一のページ**です。河﨑さんが言っていた「all episodeをテーブルビューに統合したい」の話と合わせて、Studioの画面を見ながら別途対応が必要です。

### 元に戻したいとき
転送前のコミットは `b848531` です。
```
git checkout b848531 -- index.html about.html concepts.html concept.html untagged.html inspired.html log.html contact.html
```
で復元できます（1コミットで戻せます）。

---

## 2. ショーノートの自動同期を追加（GitHub Actions）

`.github/workflows/sync-shownotes.yml` を新設しました。30分おきにスプレッドシートから
`app/shownotes.json` / `images.json` / `concepts-meta.json` / `works-meta.json` を同期します。

### なぜ必要だったか
エピソードの「存在」はRSS由来なので新作は自動で星図に出ますが、**本文（＝タグ・概念・作品の親）はスプレッドシート側にしかありません**。手動で `npm run snapshot` を打つ運用は続かず、実際に破綻していました。

- 8/2〜8/12の10日間、2話ぶん陳腐化（誰も気づかなかった）
- 8/20の作業時にも23話→24話の取りこぼしが発覚

放置すると、新エピソードが「本文なし・タグなし・概念/INSPIREDに出ない」状態でしばらく公開され続けます。

### ⚠️ Step 5（ショーノート編集画面）を作ったら、このワークフローは必ず削除すること
直接編集とポーリングを併用すると、**新しい編集を古いスプレッドシートの内容で上書きします**。LOGで同じ問題を踏んで一度廃止した実績があるので、同じ轍を踏まないでください。ymlの冒頭にもコメントを入れてあります。

### 付随して直したこと
`snapshot-shownotes.mjs` の `generatedAt` 出力を廃止しました。どのページからも読まれておらず、定期実行すると中身が同じでも毎回差分が出て空コミットが積み上がるためです（更新時刻はgitの履歴が持っています）。2回連続実行してバイト単位で同一になること（冪等性）を確認済みです。

---

## 3. CONCEPTS / INSPIRED の移植（Step 1〜3、8/12〜8/21）

Opus先生の設計に沿って3段階で進めました。**中心にあるのは「計算結果を保存しない」という判断です。**

### 考え方
```
ショーノート本文（人が書く・唯一の親）
  ├─ #タグ      → 星図のグレーノード          ← 自動計算
  ├─ ##サブタグ → 概念ページの「未タグ」        ← 自動計算
  ├─ 📚🎬📻記法 → INSPIREDの作品＋著者＋リンク  ← 自動計算
  └─ [img:key]  → 本文中の画像

人が書いた付加情報だけを別ファイルに保存
  ├─ app/concepts-meta.json … 説明・提唱者・近接/対比概念・文脈
  └─ app/works-meta.json    … 表紙画像URL・記法外の著者/URL
```

概念名・status・登場エピソード・共起は**保存していません**。`app/data.js` がショーノートから毎回計算します。だから**エピソードが増えれば概念ページもINSPIREDも勝手に最新化されます**（スナップショット不要）。

### Step 1: 計算部分の移植
`gas/Code.gs` の `computeConcepts_()` / `computeWorks_()` を `app/data.js` へ移植。
`scripts/verify-concepts-works.mjs`（`npm run verify:concepts`）で本番GASと照合し、**概念31件・作品71件、名前集合・status・登場エピソードすべて完全一致**を確認しています。

### Step 2: 手入力分の切り出し
`scripts/snapshot-curation-meta.mjs` で本番から人が書いた分だけを抽出。

- `related`/`contrast` は**辞書順で小さい方の概念にだけ片側保存**し、読み込み時に双方向展開します（両側に書くと片方消えたとき矛盾するため）
- worksの `creator`/`link_url` は、ショーノートのinline記法（`📚[題](URL)著者`）から復元できる分（47件/66件）を保存省略。シート固有の補完値だけ残しています
- ショーノートから消えた概念/作品は `exists:false` で保持し、**書いた文章を黙って消しません**
- 未使用列（`reading` 0/31、worksの`description` 0/71）は移植していません

### Step 3: 4ページを app/ へ移植
`app/concepts.html` / `concept.html` / `untagged.html` / `inspired.html`。
データ取得を `PodcastData.loadConceptsPayload()` / `loadWorksPayload()` に差し替えただけで、表示ロジックはroot版とほぼ同じです。CSSはroot共有（`../concepts.css` 等）で複製していません。

検証：概念31件（タグ済み8/未タグ23）、作品71件、**双方向展開（`テクスチャ`側にだけ保存した関連が`宝物`側からも見える）**を確認済み。

---

## 4. LOGをGitHub直書きに統合（8/12）

Googleフォーム＋スプレッドシート＋GASをやめ、`app/logpost.html` から直接 `app/logs.json` へ投稿する形にしました。`admin.js` と**同じ合言葉（`mokumoku`）・同じ暗号化トークン**を流用しています（新しい秘密は発行していません）。

- 入口：`app/index.html` 右上の小さな「●」→ `admin.html` → ヘッダーの「LOG投稿 →」
- 投稿者は「イノウエ」「カワサキ」の選択式
- 投稿・編集・削除が同じ画面でできます
- **root の `log.html` はGAS直叩きのまま**でしたが、今回の転送で `app/log.html` に飛ぶようになりました

ユーザーが本番で実際に投稿・削除を試して動作確認済みです（コミット履歴に残っています）。

---

## 5. 発見して直したバグ・地雷

### `gas/Code.gs` がリポジトリと実デプロイでズレていた（8/12・重要）
リポジトリの `gas/Code.gs` が、**実際にデプロイされているコードより大幅に古い**状態でした。CLAUDE.mdの手順（`gas/Code.gs`をGASエディタにコピペ→デプロイ）をそのままやると、**実デプロイ済みのLOG機能・概念/作品ページ機能が丸ごと消える**ところでした。

河﨑さん側にGASエディタの最新コードを貼ってもらい同期済みです。**GASを触る作業の前には毎回、最新コードを貼ってもらって同期確認してください。**

### タグ抽出が `#` と `##` を区別していなかった（8/6）
`app/data.js` のタグ抽出が `#` の連続数を見ておらず、`##`（未タグ候補）まで星図のグレーノードとして出していました。本来7個のところ31個出ていました。修正済みです。
※このとき「本番(GAS)側にも同じバグがあるかも」と報告しましたが、**これは誤りでした**。実デプロイ済みのGASは元々正しく実装されていました。

### ep9のSpotify URLがスプレッドシートで入力ミス（8/3）
番組トップのURLが入っていました。`shownotes.json` 側で空にしてRSSの `link` にフォールバックさせています（コード側でガードするとデータの誤りを隠すため不採用）。

---

## 6. 今の全体像

| データ | 置き場所 | 誰がどう編集するか |
|---|---|---|
| エピソードの存在・音声・公開日 | RSS（自動） | **Spotifyに公開するだけ** |
| ショーノート本文 | スプレッドシート → 30分おきに`app/shownotes.json`へ自動同期 | スプレッドシート（Step 5で画面化予定） |
| タグ・概念・作品 | 本文から自動計算（保存なし） | **本文に書くだけ** |
| エピソード間リンク | `app/links.json` | ● → リンク編集 ✅完成 |
| LOG | `app/logs.json` | ● → LOG投稿 ✅完成 |
| 概念の説明・提唱者・関連 | `app/concepts-meta.json` | スプレッドシート（Step 4で画面化予定） |
| 作品の表紙画像など | `app/works-meta.json` | スプレッドシート（Step 4で画面化予定） |
| テーブルの見た目調整 | `app/table-layout.json` | ● → レイアウト ✅完成 |

**GASにまだ依存しているもの**：ショーノート本文（自動同期で救済済み）、概念/作品の手入力分、`episodes.html`。

---

## 7. コマンド一覧

```
npm run snapshot          # スプレッドシート → shownotes.json / images.json
npm run snapshot:curation # 本番GAS → concepts-meta.json / works-meta.json
npm run verify            # app/data.jsが本番と同等か（エピソード数・タグ・画像・リンク）
npm run verify:concepts   # 概念・作品が本番GASと完全一致するか
node scripts/make-redirects.mjs --dry   # 転送HTMLの中身を確認（書き込まない）
```
※ verify系はローカルに静的サーバーが要ります：`python -m http.server 8123`

---

## 8. 次にやること

1. **`episodes.html` / Studio埋め込みの扱いを決める**（GAS依存が残る唯一のページ。河﨑さんの「all episodeをテーブルビューに統合したい」と合流させる）
2. **Step 4：概念・作品の編集画面**（`app/curation.html`）。`logpost.js` と同じ合言葉方式で。要件は「31件を1画面のリストでその場編集」「未入力件数バッジと絞り込み」「近接/対比概念は既存概念からの選択式」（今スプレッドシートで起きている"打ち間違えると黙って無視される"問題が消えます）
3. **Step 5：ショーノート編集画面**（同じ`curation.html`にタブ追加）。**ここでGASが引退します。着手したら sync-shownotes.yml を必ず削除すること**
4. `QUESTIONS.md` の残項目（GAS_URLの最新デプロイ一致確認／「参考番組」シートの用途）

---

## 9. 運用ルール（継続）

- 本番公開ブランチは `feature/logo-deco`（mainは削除済み）
- **`app/` 配下を直す。ルート直下のHTMLはもう転送用の殻**
- `app/links.json` / `app/logs.json` / `app/table-layout.json` は管理画面から直接PUTされる。ローカルで触る前に必ず最新をpull/curlし、**ユーザーの保存値を上書きしない**
- `gas/Code.gs` は実デプロイとズレやすい。触る前に最新コードを貼ってもらう
- **commit/pushしたら同じタイミングでHANDOVERも更新する。** 相手はpullするまで見えない前提で運用する
